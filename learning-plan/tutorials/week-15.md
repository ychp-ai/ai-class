<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 15 周教程：Eval 与 Trace

> 计划来源：[05-integration-evaluation-delivery.md](../stages/05-integration-evaluation-delivery.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 15 周成果](../../deliverables/week-15/README.md)。

## 本周先理解

### 为什么学

用固定数据、统一指标和跨服务证据判断改动是否真的更好。

### 这是什么

Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：定义 RAG 和 Agent 指标

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“定义 RAG 和 Agent 指标”推进为可检查成果；如果跳过，后续会缺少“指标有明确判断方法”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“定义 RAG 和 Agent 指标”是这个核心能力的最小学习单元：你会通过“每个指标写清计算公式、数据来源和好坏方向，避免“整体感觉不错””观察输入如何转化为“指标有明确判断方法”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：每个指标写清计算公式、数据来源和好坏方向，避免“整体感觉不错”
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)

**怎么验证学会了**

- 必交证据：指标有明确判断方法。
- Teach-back：不看资料解释“定义 RAG 和 Agent 指标”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“定义 RAG 和 Agent 指标”后，应能观察到“指标有明确判断方法”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)
- 指定章节/条目：Retrieval Augmented Generation → Advisors / ETL Pipeline / Retrieval flow
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：每个指标写清计算公式、数据来源和好坏方向，避免“整体感觉不错”
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
def run_case(input_data: dict) -> dict:
    if not input_data:
        return {"status": "FAILED", "error": "EMPTY_INPUT"}
    # Replace this line with the lesson-specific call.
    result = {"observed": input_data, "status": "SUCCEEDED"}
    return result

print(run_case({"case": "normal"}))
print(run_case({}))  # failure case
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：指标有明确判断方法。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：准备 20 条 RAG 问题

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“准备 20 条 RAG 问题”推进为可检查成果；如果跳过，后续会缺少“覆盖事实、路径、总结和拒答”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“准备 20 条 RAG 问题”是这个核心能力的最小学习单元：你会通过“在看系统结果前先写预期答案与来源；加入无答案和相似干扰问题”观察输入如何转化为“覆盖事实、路径、总结和拒答”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：在看系统结果前先写预期答案与来源；加入无答案和相似干扰问题
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[NIST AI Resource Center](https://airc.nist.gov/)

**怎么验证学会了**

- 必交证据：覆盖事实、路径、总结和拒答。
- Teach-back：不看资料解释“准备 20 条 RAG 问题”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“准备 20 条 RAG 问题”后，应能观察到“覆盖事实、路径、总结和拒答”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[NIST AI Resource Center](https://airc.nist.gov/)
- 指定章节/条目：Retrieval Augmented Generation → Advisors / ETL Pipeline / Retrieval flow
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：在看系统结果前先写预期答案与来源；加入无答案和相似干扰问题
- 参考资料/文档产出：

- 主题：准备 20 条 RAG 问题
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“覆盖事实、路径、总结和拒答”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：覆盖事实、路径、总结和拒答。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：准备 10 条 Agent 任务

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“准备 10 条 Agent 任务”推进为可检查成果；如果跳过，后续会缺少“覆盖只读、修改、失败和拒绝”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“准备 10 条 Agent 任务”是这个核心能力的最小学习单元：你会通过“每个任务提供初始工作区、允许副作用和机器可执行的验收命令”观察输入如何转化为“覆盖只读、修改、失败和拒绝”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：每个任务提供初始工作区、允许副作用和机器可执行的验收命令
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)

**怎么验证学会了**

- 必交证据：覆盖只读、修改、失败和拒绝。
- Teach-back：不看资料解释“准备 10 条 Agent 任务”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“准备 10 条 Agent 任务”后，应能观察到“覆盖只读、修改、失败和拒绝”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)
- 指定章节/条目：该链接指向的“OpenAI Evals 指南”整篇专题/章节；重点阅读与“准备 10 条 Agent 任务”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：每个任务提供初始工作区、允许副作用和机器可执行的验收命令
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
def run_case(input_data: dict) -> dict:
    if not input_data:
        return {"status": "FAILED", "error": "EMPTY_INPUT"}
    # Replace this line with the lesson-specific call.
    result = {"observed": input_data, "status": "SUCCEEDED"}
    return result

print(run_case({"case": "normal"}))
print(run_case({}))  # failure case
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：覆盖只读、修改、失败和拒绝。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：统一 Trace、日志和成本字段

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“统一 Trace、日志和成本字段”推进为可检查成果；如果跳过，后续会缺少“Java/Python 关联同一任务”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“统一 Trace、日志和成本字段”是这个核心能力的最小学习单元：你会通过“使用同一任务 ID 贯穿服务；日志字段稳定，敏感内容脱敏且大输出截断”观察输入如何转化为“Java/Python 关联同一任务”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：使用同一任务 ID 贯穿服务；日志字段稳定，敏感内容脱敏且大输出截断
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/specs/semconv/)

**怎么验证学会了**

- 必交证据：Java/Python 关联同一任务。
- Teach-back：不看资料解释“统一 Trace、日志和成本字段”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“统一 Trace、日志和成本字段”后，应能观察到“Java/Python 关联同一任务”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/specs/semconv/)
- 指定章节/条目：Trace/Log semantic conventions → Span、Attributes、Context propagation
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：使用同一任务 ID 贯穿服务；日志字段稳定，敏感内容脱敏且大输出截断
- 参考资料/文档产出：

- 主题：统一 Trace、日志和成本字段
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“Java/Python 关联同一任务”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Java/Python 关联同一任务。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：跑第一轮 Baseline

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“跑第一轮 Baseline”推进为可检查成果；如果跳过，后续会缺少“保存完整原始结果”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“跑第一轮 Baseline”是这个核心能力的最小学习单元：你会通过“冻结模型、Prompt、数据和参数；保存全部原始结果，包括超时和失败”观察输入如何转化为“保存完整原始结果”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：冻结模型、Prompt、数据和参数；保存全部原始结果，包括超时和失败
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)

**怎么验证学会了**

- 必交证据：保存完整原始结果。
- Teach-back：不看资料解释“跑第一轮 Baseline”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“跑第一轮 Baseline”后，应能观察到“保存完整原始结果”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)
- 指定章节/条目：该链接指向的“OpenAI Evals 指南”整篇专题/章节；重点阅读与“跑第一轮 Baseline”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：冻结模型、Prompt、数据和参数；保存全部原始结果，包括超时和失败
- 参考资料/文档产出：

- 主题：跑第一轮 Baseline
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“保存完整原始结果”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：保存完整原始结果。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：只调整一个主要变量并复测

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“只调整一个主要变量并复测”推进为可检查成果；如果跳过，后续会缺少“有可解释对照”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“只调整一个主要变量并复测”是这个核心能力的最小学习单元：你会通过“选择最可能影响失败的变量，记录假设；不要为了提升分数删除困难用例”观察输入如何转化为“有可解释对照”。它既包含可观察结果，也包含至少一个失败或不适用边界。重点边界来自课程原计划：不要为了提升分数删除困难用例。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：选择最可能影响失败的变量，记录假设；不要为了提升分数删除困难用例
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)

**怎么验证学会了**

- 必交证据：有可解释对照。
- Teach-back：不看资料解释“只调整一个主要变量并复测”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“只调整一个主要变量并复测”后，应能观察到“有可解释对照”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)
- 指定章节/条目：该链接指向的“OpenAI Eval 最佳实践”整篇专题/章节；重点阅读与“只调整一个主要变量并复测”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：选择最可能影响失败的变量，记录假设；不要为了提升分数删除困难用例
- 参考资料/文档产出：

- 主题：只调整一个主要变量并复测
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“有可解释对照”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：有可解释对照。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：分析失败并形成报告

**为什么学**

本周要用固定数据、统一指标和跨服务证据判断改动是否真的更好。本节把“分析失败并形成报告”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。今天的“分析失败并形成报告”是这个核心能力的最小学习单元：你会通过“按检索、生成、工具、流程、环境分类失败，并给出下一步验证而非空泛结论”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：按检索、生成、工具、流程、环境分类失败，并给出下一步验证而非空泛结论
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[NIST 生成式 AI 风险框架](https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.600-1.pdf)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“分析失败并形成报告”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Eval 是对固定样例按明确口径重复评分；Trace 是把一次运行的服务、模型、工具和状态变化关联起来的可观测记录。
- 预测：完成“分析失败并形成报告”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[NIST 生成式 AI 风险框架](https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.600-1.pdf)
- 指定章节/条目：该链接指向的“NIST 生成式 AI 风险框架”整篇专题/章节；重点阅读与“分析失败并形成报告”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：按检索、生成、工具、流程、环境分类失败，并给出下一步验证而非空泛结论
- 参考资料/文档产出：

- 主题：分析失败并形成报告
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
