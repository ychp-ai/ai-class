<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 12 周教程：Claude Agent SDK

> 计划来源：[04-claude-agent-sdk.md](../stages/04-claude-agent-sdk.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 12 周成果](../../deliverables/week-12/README.md)。

## 本周先理解

### 为什么学

掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。

### 这是什么

Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。

### 本周语言定位

必须使用 Python 3.11+、类型标注、Pydantic 和 pytest，重点观察 Agent 抽象、异步事件、状态与恢复。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：区分 Agent SDK、Anthropic Client SDK 和 Claude Code

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“区分 Agent SDK、Anthropic Client SDK 和 Claude Code”推进为可检查成果；如果跳过，后续会缺少“写出定位与使用场景”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“区分 Agent SDK、Anthropic Client SDK 和 Claude Code”是这个核心能力的最小学习单元：你会通过“分别从调用者、Agent 循环、内置工具和部署方式比较，不要只记产品名称”观察输入如何转化为“写出定位与使用场景”。它既包含可观察结果，也包含至少一个失败或不适用边界。重点边界来自课程原计划：不要只记产品名称。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：分别从调用者、Agent 循环、内置工具和部署方式比较，不要只记产品名称
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Overview](https://code.claude.com/docs/en/agent-sdk/overview)

**怎么验证学会了**

- 必交证据：写出定位与使用场景。
- Teach-back：不看资料解释“区分 Agent SDK、Anthropic Client SDK 和 Claude Code”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“区分 Agent SDK、Anthropic Client SDK 和 Claude Code”后，应能观察到“写出定位与使用场景”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Overview](https://code.claude.com/docs/en/agent-sdk/overview)
- 指定章节/条目：Agent SDK → Overview / Streaming output → Message types、Final result
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：分别从调用者、Agent 循环、内置工具和部署方式比较，不要只记产品名称
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：写出定位与使用场景。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：使用 `query()` 和流式消息

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“使用 `query()` 和流式消息”推进为可检查成果；如果跳过，后续会缺少“打印工具调用和最终结果”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“使用 `query()` 和流式消息”是这个核心能力的最小学习单元：你会通过“打印消息类型、工具事件和最终状态；不要把所有消息直接当最终文本处理”观察输入如何转化为“打印工具调用和最终结果”。它既包含可观察结果，也包含至少一个失败或不适用边界。重点边界来自课程原计划：不要把所有消息直接当最终文本处理。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：打印消息类型、工具事件和最终状态；不要把所有消息直接当最终文本处理
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Streaming](https://code.claude.com/docs/en/agent-sdk/streaming-output)

**怎么验证学会了**

- 必交证据：打印工具调用和最终结果。
- Teach-back：不看资料解释“使用 `query()` 和流式消息”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“使用 `query()` 和流式消息”后，应能观察到“打印工具调用和最终结果”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Streaming](https://code.claude.com/docs/en/agent-sdk/streaming-output)
- 指定章节/条目：Agent SDK → Overview / Streaming output → Message types、Final result
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：打印消息类型、工具事件和最终状态；不要把所有消息直接当最终文本处理
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：打印工具调用和最终结果。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：使用 Read、Glob、Grep

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“使用 Read、Glob、Grep”推进为可检查成果；如果跳过，后续会缺少“输出有文件证据的代码分析”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“使用 Read、Glob、Grep”是这个核心能力的最小学习单元：你会通过“只开放只读工具，要求每个分析结论给出文件路径；观察 Agent 的搜索策略”观察输入如何转化为“输出有文件证据的代码分析”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：只开放只读工具，要求每个分析结论给出文件路径；观察 Agent 的搜索策略
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)

**怎么验证学会了**

- 必交证据：输出有文件证据的代码分析。
- Teach-back：不看资料解释“使用 Read、Glob、Grep”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“使用 Read、Glob、Grep”后，应能观察到“输出有文件证据的代码分析”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)
- 指定章节/条目：Tools reference → Read、Glob、Grep、Edit、Bash
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：只开放只读工具，要求每个分析结论给出文件路径；观察 Agent 的搜索策略
- 参考资料/文档产出：

- 主题：使用 Read、Glob、Grep
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“输出有文件证据的代码分析”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：输出有文件证据的代码分析。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：在练习目录中使用 Edit

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“在练习目录中使用 Edit”推进为可检查成果；如果跳过，后续会缺少“只修改指定文件”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“在练习目录中使用 Edit”是这个核心能力的最小学习单元：你会通过“使用专用 Demo 和版本控制；修改前记录基线 Diff，限制允许文件范围”观察输入如何转化为“只修改指定文件”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：使用专用 Demo 和版本控制；修改前记录基线 Diff，限制允许文件范围
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)

**怎么验证学会了**

- 必交证据：只修改指定文件。
- Teach-back：不看资料解释“在练习目录中使用 Edit”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“在练习目录中使用 Edit”后，应能观察到“只修改指定文件”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)
- 指定章节/条目：Tools reference → Read、Glob、Grep、Edit、Bash
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：使用专用 Demo 和版本控制；修改前记录基线 Diff，限制允许文件范围
- 参考资料/文档产出：

- 主题：在练习目录中使用 Edit
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“只修改指定文件”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：只修改指定文件。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：使用 Bash 运行单元测试

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“使用 Bash 运行单元测试”推进为可检查成果；如果跳过，后续会缺少“保存命令、退出码和结果”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“使用 Bash 运行单元测试”是这个核心能力的最小学习单元：你会通过“只允许明确的测试命令；记录工作目录、命令、退出码和截断后的输出”观察输入如何转化为“保存命令、退出码和结果”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：只允许明确的测试命令；记录工作目录、命令、退出码和截断后的输出
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)

**怎么验证学会了**

- 必交证据：保存命令、退出码和结果。
- Teach-back：不看资料解释“使用 Bash 运行单元测试”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“使用 Bash 运行单元测试”后，应能观察到“保存命令、退出码和结果”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Tools Reference](https://code.claude.com/docs/en/tools-reference)
- 指定章节/条目：Tools reference → Read、Glob、Grep、Edit、Bash
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：只允许明确的测试命令；记录工作目录、命令、退出码和截断后的输出
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：保存命令、退出码和结果。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：Session 恢复和分叉

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“Session 恢复和分叉”推进为可检查成果；如果跳过，后续会缺少“能继续旧任务并创建分支”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“Session 恢复和分叉”是这个核心能力的最小学习单元：你会通过“设计两个不同后续方向，验证恢复继续原状态、分叉不污染原会话”观察输入如何转化为“能继续旧任务并创建分支”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：设计两个不同后续方向，验证恢复继续原状态、分叉不污染原会话
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Sessions](https://code.claude.com/docs/en/agent-sdk/sessions)

**怎么验证学会了**

- 必交证据：能继续旧任务并创建分支。
- Teach-back：不看资料解释“Session 恢复和分叉”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“Session 恢复和分叉”后，应能观察到“能继续旧任务并创建分支”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Sessions](https://code.claude.com/docs/en/agent-sdk/sessions)
- 指定章节/条目：Persistence → Threads、Checkpoints、Get state history、Replay
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：设计两个不同后续方向，验证恢复继续原状态、分叉不污染原会话
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：能继续旧任务并创建分支。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：完成自动修复 Agent

**为什么学**

本周要掌握隔离环境中的文件、代码和 Shell 执行闭环，同时看清会话和工具事件。本节把“完成自动修复 Agent”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。今天的“完成自动修复 Agent”是这个核心能力的最小学习单元：你会通过“使用“修复前失败、最小修改、修复后通过”闭环，人工审查所有文件变化”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：使用“修复前失败、最小修改、修复后通过”闭环，人工审查所有文件变化
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[成果与提交标准](../06-deliverable-standards.md)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“完成自动修复 Agent”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Claude Agent SDK 提供带 Agent 循环和内置工具的执行型运行时；它不同于普通模型 Client SDK，也不是宿主机全权限执行器。
- 预测：完成“完成自动修复 Agent”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[成果与提交标准](../06-deliverable-standards.md)
- 指定章节/条目：该链接指向的“成果与提交标准”整篇专题/章节；重点阅读与“完成自动修复 Agent”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：使用“修复前失败、最小修改、修复后通过”闭环，人工审查所有文件变化
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
