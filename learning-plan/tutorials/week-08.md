<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 08 周教程：手写 Java Agent

> 计划来源：[02-java-llm-rag-agent.md](../stages/02-java-llm-rag-agent.md)。阶段文档决定课程任务和验收，[第 08 周公共成果要求](../../deliverables/week-08/README.md)定义门禁；学习状态只记录在个人学习仓库。

## 本周先理解

### 为什么学

亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。

### 这是什么

Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。

### 本周语言定位

必须使用 Java 21、Maven、Spring Boot/Spring AI，并把前四周的原生能力映射到 Java 实现、测试和错误处理。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：学习 Plan–Act–Observe

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“学习 Plan–Act–Observe”推进为可检查成果；如果跳过，后续会缺少“画出 Agent 循环”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“学习 Plan–Act–Observe”是这个核心能力的最小学习单元：你会通过“用纸面模拟两轮循环，明确完成、失败和继续三个判断，不急着写代码”观察输入如何转化为“画出 Agent 循环”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：用纸面模拟两轮循环，明确完成、失败和继续三个判断，不急着写代码
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[ReAct 论文](https://arxiv.org/abs/2210.03629)

**怎么验证学会了**

- 必交证据：画出 Agent 循环。
- Teach-back：不看资料解释“学习 Plan–Act–Observe”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“学习 Plan–Act–Observe”后，应能观察到“画出 Agent 循环”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[ReAct 论文](https://arxiv.org/abs/2210.03629)
- 指定章节/条目：ReAct paper → Abstract、Method、Figure 1、Experiments
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：用纸面模拟两轮循环，明确完成、失败和继续三个判断，不急着写代码
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
for (int step = 0; step < MAX_STEPS; step++) {
    Decision decision = planner.next(state);
    if (decision.done()) return RunResult.succeeded(state);
    ToolResult observed = toolExecutor.executeValidated(decision.toolCall());
    state = state.append(decision, observed);
}
return RunResult.failed("MAX_STEPS_EXCEEDED");
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：画出 Agent 循环。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：生成结构化任务计划

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“生成结构化任务计划”推进为可检查成果；如果跳过，后续会缺少“计划可被 Java 对象解析”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“生成结构化任务计划”是这个核心能力的最小学习单元：你会通过“步骤必须包含动作和验收条件；限制步骤数量并测试无法解析的计划”观察输入如何转化为“计划可被 Java 对象解析”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：步骤必须包含动作和验收条件；限制步骤数量并测试无法解析的计划
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI Structured Output](https://docs.spring.io/spring-ai/reference/api/structured-output.html)

**怎么验证学会了**

- 必交证据：计划可被 Java 对象解析。
- Teach-back：不看资料解释“生成结构化任务计划”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“生成结构化任务计划”后，应能观察到“计划可被 Java 对象解析”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI Structured Output](https://docs.spring.io/spring-ai/reference/api/structured-output.html)
- 指定章节/条目：该链接指向的“Spring AI Structured Output”整篇专题/章节；重点阅读与“生成结构化任务计划”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：步骤必须包含动作和验收条件；限制步骤数量并测试无法解析的计划
- 参考资料/文档产出：

- 主题：生成结构化任务计划
- 建议字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间
- 示例结论：已按统一口径产出“计划可被 Java 对象解析”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：计划可被 Java 对象解析。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：设计任务、步骤和运行日志对象

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“设计任务、步骤和运行日志对象”推进为可检查成果；如果跳过，后续会缺少“状态转换明确”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“设计任务、步骤和运行日志对象”是这个核心能力的最小学习单元：你会通过“先定义状态机，避免用多个布尔值组合出矛盾状态；为状态转换写测试”观察输入如何转化为“状态转换明确”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：先定义状态机，避免用多个布尔值组合出矛盾状态；为状态转换写测试
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[W3C SCXML 状态机规范](https://www.w3.org/TR/scxml/)

**怎么验证学会了**

- 必交证据：状态转换明确。
- Teach-back：不看资料解释“设计任务、步骤和运行日志对象”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“设计任务、步骤和运行日志对象”后，应能观察到“状态转换明确”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[W3C SCXML 状态机规范](https://www.w3.org/TR/scxml/)
- 指定章节/条目：Trace/Log semantic conventions → Span、Attributes、Context propagation
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：先定义状态机，避免用多个布尔值组合出矛盾状态；为状态转换写测试
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
@RestController
final class ChatController {
    private final ChatClient chatClient;

    ChatController(ChatClient.Builder builder) { this.chatClient = builder.build(); }

    @PostMapping("/api/chat")
    ChatResponse chat(@Valid @RequestBody ChatRequest request) {
        String content = chatClient.prompt().user(request.message()).call().content();
        return new ChatResponse(content == null ? "" : content);
    }
}
record ChatRequest(@NotBlank String message) {}
record ChatResponse(String content) {}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：状态转换明确。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：调用文件与检索工具完成任务

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“调用文件与检索工具完成任务”推进为可检查成果；如果跳过，后续会缺少“完成只读分析任务”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“调用文件与检索工具完成任务”是这个核心能力的最小学习单元：你会通过“只做只读任务，把 Agent 与工具本身的错误分开记录”观察输入如何转化为“完成只读分析任务”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：只做只读任务，把 Agent 与工具本身的错误分开记录
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI Tool Calling](https://docs.spring.io/spring-ai/reference/api/tools.html)

**怎么验证学会了**

- 必交证据：完成只读分析任务。
- Teach-back：不看资料解释“调用文件与检索工具完成任务”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“调用文件与检索工具完成任务”后，应能观察到“完成只读分析任务”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI Tool Calling](https://docs.spring.io/spring-ai/reference/api/tools.html)
- 指定章节/条目：该链接指向的“Spring AI Tool Calling”整篇专题/章节；重点阅读与“调用文件与检索工具完成任务”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：只做只读任务，把 Agent 与工具本身的错误分开记录
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record FileRequest(String path) {}

final class SafeFileTools {
    private final Path root = Path.of("sandbox-workspaces/demo").toAbsolutePath();

    @Tool(description = "Read a UTF-8 file inside the demo workspace")
    String readFile(FileRequest request) throws IOException {
        Path target = root.resolve(request.path()).normalize();
        if (!target.startsWith(root)) throw new IllegalArgumentException("PATH_OUTSIDE_ROOT");
        return Files.readString(target);
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成只读分析任务。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：记录模型、工具、观察和结果

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“记录模型、工具、观察和结果”推进为可检查成果；如果跳过，后续会缺少“可复盘每一步”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“记录模型、工具、观察和结果”是这个核心能力的最小学习单元：你会通过“日志保存必要摘要和关联 ID，不记录密钥或无限增长的完整上下文”观察输入如何转化为“可复盘每一步”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：日志保存必要摘要和关联 ID，不记录密钥或无限增长的完整上下文
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenTelemetry Logs](https://opentelemetry.io/docs/specs/otel/logs/)

**怎么验证学会了**

- 必交证据：可复盘每一步。
- Teach-back：不看资料解释“记录模型、工具、观察和结果”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“记录模型、工具、观察和结果”后，应能观察到“可复盘每一步”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenTelemetry Logs](https://opentelemetry.io/docs/specs/otel/logs/)
- 指定章节/条目：该链接指向的“OpenTelemetry Logs”整篇专题/章节；重点阅读与“记录模型、工具、观察和结果”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：日志保存必要摘要和关联 ID，不记录密钥或无限增长的完整上下文
- 参考资料/文档产出：

- 主题：记录模型、工具、观察和结果
- 建议字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间
- 示例结论：已按统一口径产出“可复盘每一步”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：可复盘每一步。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：最大步数、超时、重试和人工中断

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“最大步数、超时、重试和人工中断”推进为可检查成果；如果跳过，后续会缺少“Agent 不会无限循环”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“最大步数、超时、重试和人工中断”是这个核心能力的最小学习单元：你会通过“人为制造循环与工具失败，验证限制真正生效；重试只针对可恢复错误”观察输入如何转化为“Agent 不会无限循环”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：人为制造循环与工具失败，验证限制真正生效；重试只针对可恢复错误
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)

**怎么验证学会了**

- 必交证据：Agent 不会无限循环。
- Teach-back：不看资料解释“最大步数、超时、重试和人工中断”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“最大步数、超时、重试和人工中断”后，应能观察到“Agent 不会无限循环”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)
- 指定章节/条目：该链接指向的“Resilience4j Guide”整篇专题/章节；重点阅读与“最大步数、超时、重试和人工中断”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：人为制造循环与工具失败，验证限制真正生效；重试只针对可恢复错误
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
@RestController
final class ChatController {
    private final ChatClient chatClient;

    ChatController(ChatClient.Builder builder) { this.chatClient = builder.build(); }

    @PostMapping("/api/chat")
    ChatResponse chat(@Valid @RequestBody ChatRequest request) {
        String content = chatClient.prompt().user(request.message()).call().content();
        return new ChatResponse(content == null ? "" : content);
    }
}
record ChatRequest(@NotBlank String message) {}
record ChatResponse(String content) {}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Agent 不会无限循环。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：完成 README 整理 Agent

**为什么学**

本周要亲手实现规划—工具—观察循环，理解 Agent 的状态、停止条件和副作用边界。本节把“完成 README 整理 Agent”推进为可检查成果；如果跳过，后续会缺少“完成阶段提交”这项关键证据。

**这个是什么**

本周核心概念是：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。今天的“完成 README 整理 Agent”是这个核心能力的最小学习单元：你会通过“演示一次成功、一次失败和一次超限，按阶段出口条件自检”观察输入如何转化为“完成阶段提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：演示一次成功、一次失败和一次超限，按阶段出口条件自检
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[成果与提交标准](../06-deliverable-standards.md)

**怎么验证学会了**

- 必交证据：完成阶段提交。
- Teach-back：不看资料解释“完成 README 整理 Agent”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Agent 是围绕目标反复选择动作、观察结果并决定下一步的受控运行循环；它不是拥有无限权限的自主程序。
- 预测：完成“完成 README 整理 Agent”后，应能观察到“完成阶段提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[成果与提交标准](../06-deliverable-standards.md)
- 指定章节/条目：该链接指向的“成果与提交标准”整篇专题/章节；重点阅读与“完成 README 整理 Agent”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：演示一次成功、一次失败和一次超限，按阶段出口条件自检
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
for (int step = 0; step < MAX_STEPS; step++) {
    Decision decision = planner.next(state);
    if (decision.done()) return RunResult.succeeded(state);
    ToolResult observed = toolExecutor.executeValidated(decision.toolCall());
    state = state.append(decision, observed);
}
return RunResult.failed("MAX_STEPS_EXCEEDED");
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成阶段提交。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
