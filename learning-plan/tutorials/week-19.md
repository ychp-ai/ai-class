<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 19 周教程：Model Gateway

> 计划来源：[06-ai-platform-core.md](../stages/06-ai-platform-core.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 19 周成果](../../deliverables/week-19/README.md)。

## 本周先理解

### 为什么学

集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。

### 这是什么

Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。

### 本周语言定位

Java 承担平台控制面与治理，Python 承担已授权的 Agent/Workflow 执行；TypeScript 只在管理流程需要时提供最小界面。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：Provider 与 Model Alias

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“Provider 与 Model Alias”推进为可检查成果；如果跳过，后续会缺少“可切换别名映射”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“Provider 与 Model Alias”是这个核心能力的最小学习单元：你会通过“业务和 Agent 引用平台别名，不直接散落供应商模型名；凭证只存 Secret 引用”观察输入如何转化为“可切换别名映射”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：业务和 Agent 引用平台别名，不直接散落供应商模型名；凭证只存 Secret 引用
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)

**怎么验证学会了**

- 必交证据：可切换别名映射。
- Teach-back：不看资料解释“Provider 与 Model Alias”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“Provider 与 Model Alias”后，应能观察到“可切换别名映射”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)
- 指定章节/条目：该链接指向的“Spring AI ChatClient”整篇专题/章节；重点阅读与“Provider 与 Model Alias”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：业务和 Agent 引用平台别名，不直接散落供应商模型名；凭证只存 Secret 引用
- 参考资料/文档产出：

- 主题：Provider 与 Model Alias
- 建议字段：对象；Owner/角色；输入或来源；规则/约束；风险；证据；状态；下一步
- 示例结论：已按统一口径产出“可切换别名映射”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：可切换别名映射。
- 完整字段：对象；Owner/角色；输入或来源；规则/约束；风险；证据；状态；下一步。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：统一请求、响应与错误

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“统一请求、响应与错误”推进为可检查成果；如果跳过，后续会缺少“两个 Provider 适配同一接口”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“统一请求、响应与错误”是这个核心能力的最小学习单元：你会通过“统一 Usage、FinishReason、ToolCall 和流式事件；保留供应商扩展字段的边界”观察输入如何转化为“两个 Provider 适配同一接口”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：统一 Usage、FinishReason、ToolCall 和流式事件；保留供应商扩展字段的边界
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI API Quickstart](https://platform.openai.com/docs/quickstart)

**怎么验证学会了**

- 必交证据：两个 Provider 适配同一接口。
- Teach-back：不看资料解释“统一请求、响应与错误”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“统一请求、响应与错误”后，应能观察到“两个 Provider 适配同一接口”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI API Quickstart](https://platform.openai.com/docs/quickstart)
- 指定章节/条目：该链接指向的“OpenAI API Quickstart”整篇专题/章节；重点阅读与“统一请求、响应与错误”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：统一 Usage、FinishReason、ToolCall 和流式事件；保留供应商扩展字段的边界
- 参考资料/文档产出：

- 主题：统一请求、响应与错误
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“两个 Provider 适配同一接口”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：两个 Provider 适配同一接口。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：本地模型、量化与微调边界

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“本地模型、量化与微调边界”推进为可检查成果；如果跳过，后续会缺少“本地推理实验和选型 ADR”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“本地模型、量化与微调边界”是这个核心能力的最小学习单元：你会通过“运行一个本地开源模型并记录内存/显存、吞吐和量化影响；写清 RAG、Prompt 与 LoRA/PEFT 的选择条件”观察输入如何转化为“本地推理实验和选型 ADR”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：运行一个本地开源模型并记录内存/显存、吞吐和量化影响；写清 RAG、Prompt 与 LoRA/PEFT 的选择条件
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Hugging Face Quantization](https://huggingface.co/docs/transformers/main/en/quantization/overview)

**怎么验证学会了**

- 必交证据：本地推理实验和选型 ADR。
- Teach-back：不看资料解释“本地模型、量化与微调边界”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“本地模型、量化与微调边界”后，应能观察到“本地推理实验和选型 ADR”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Hugging Face Quantization](https://huggingface.co/docs/transformers/main/en/quantization/overview)
- 指定章节/条目：该链接指向的“Hugging Face Quantization”整篇专题/章节；重点阅读与“本地模型、量化与微调边界”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：运行一个本地开源模型并记录内存/显存、吞吐和量化影响；写清 RAG、Prompt 与 LoRA/PEFT 的选择条件
- 参考资料/文档产出：

- 主题：本地模型、量化与微调边界
- 建议字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间
- 示例结论：已按统一口径产出“本地推理实验和选型 ADR”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：本地推理实验和选型 ADR。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：超时、重试、熔断与 Cache

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“超时、重试、熔断与 Cache”推进为可检查成果；如果跳过，后续会缺少“故障与 Cache 隔离测试”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“超时、重试、熔断与 Cache”是这个核心能力的最小学习单元：你会通过“重试只覆盖安全错误；Cache Key 包含模型、Prompt、Tool Schema 和参数，敏感内容不跨项目复用”观察输入如何转化为“故障与 Cache 隔离测试”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：重试只覆盖安全错误；Cache Key 包含模型、Prompt、Tool Schema 和参数，敏感内容不跨项目复用
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)

**怎么验证学会了**

- 必交证据：故障与 Cache 隔离测试。
- Teach-back：不看资料解释“超时、重试、熔断与 Cache”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“超时、重试、熔断与 Cache”后，应能观察到“故障与 Cache 隔离测试”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)
- 指定章节/条目：该链接指向的“Resilience4j Guide”整篇专题/章节；重点阅读与“超时、重试、熔断与 Cache”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：重试只覆盖安全错误；Cache Key 包含模型、Prompt、Tool Schema 和参数，敏感内容不跨项目复用
- 参考资料/文档产出：

- 主题：超时、重试、熔断与 Cache
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“故障与 Cache 隔离测试”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：故障与 Cache 隔离测试。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：路由与 Fallback

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“路由与 Fallback”推进为可检查成果；如果跳过，后续会缺少“主模型失败可受控降级”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“路由与 Fallback”是这个核心能力的最小学习单元：你会通过“按能力、成本、延迟和数据策略路由；Fallback 前确认 Schema、上下文和工具能力兼容”观察输入如何转化为“主模型失败可受控降级”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：按能力、成本、延迟和数据策略路由；Fallback 前确认 Schema、上下文和工具能力兼容
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Google SRE：Handling Overload](https://sre.google/sre-book/handling-overload/)

**怎么验证学会了**

- 必交证据：主模型失败可受控降级。
- Teach-back：不看资料解释“路由与 Fallback”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“路由与 Fallback”后，应能观察到“主模型失败可受控降级”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Google SRE：Handling Overload](https://sre.google/sre-book/handling-overload/)
- 指定章节/条目：该链接指向的“Google SRE：Handling Overload”整篇专题/章节；重点阅读与“路由与 Fallback”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：按能力、成本、延迟和数据策略路由；Fallback 前确认 Schema、上下文和工具能力兼容
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“路由与 Fallback”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "主模型失败可受控降级");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：主模型失败可受控降级。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：配额、限流与成本预算

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“配额、限流与成本预算”推进为可检查成果；如果跳过，后续会缺少“超限请求不会调用模型”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“配额、限流与成本预算”是这个核心能力的最小学习单元：你会通过“同时设置请求、Token、并发和单次预算；超额返回明确业务错误”观察输入如何转化为“超限请求不会调用模型”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：同时设置请求、Token、并发和单次预算；超额返回明确业务错误
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[RFC 9333：RateLimit 字段](https://www.rfc-editor.org/rfc/rfc9333.html)

**怎么验证学会了**

- 必交证据：超限请求不会调用模型。
- Teach-back：不看资料解释“配额、限流与成本预算”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“配额、限流与成本预算”后，应能观察到“超限请求不会调用模型”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[RFC 9333：RateLimit 字段](https://www.rfc-editor.org/rfc/rfc9333.html)
- 指定章节/条目：RFC 9333 → RateLimit-Limit、RateLimit-Remaining、RateLimit-Reset
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：同时设置请求、Token、并发和单次预算；超额返回明确业务错误
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“配额、限流与成本预算”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "超限请求不会调用模型");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：超限请求不会调用模型。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：基准与路由报告

**为什么学**

本周要集中处理 Provider 差异、路由、配额、降级和成本，避免策略散落在业务代码。本节把“基准与路由报告”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。今天的“基准与路由报告”是这个核心能力的最小学习单元：你会通过“用固定任务比较质量、P95 延迟、Token 和成本，避免只比回答观感”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：用固定任务比较质量、P95 延迟、Token 和成本，避免只比回答观感
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“基准与路由报告”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Model Gateway 用统一请求响应和模型别名隔离供应商，并在明确兼容条件下执行超时、重试、缓存、路由与 Fallback。
- 预测：完成“基准与路由报告”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)
- 指定章节/条目：该链接指向的“OpenAI Eval 最佳实践”整篇专题/章节；重点阅读与“基准与路由报告”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：用固定任务比较质量、P95 延迟、Token 和成本，避免只比回答观感
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“基准与路由报告”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "完成本周提交");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
