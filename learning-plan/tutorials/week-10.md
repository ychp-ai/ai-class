<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 10 周教程：LangChain RAG

> 计划来源：[03-langchain-langgraph.md](../stages/03-langchain-langgraph.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 10 周成果](../../deliverables/week-10/README.md)。

## 本周先理解

### 为什么学

在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。

### 这是什么

LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。

### 本周语言定位

必须使用 Python 3.11+、类型标注、Pydantic 和 pytest，重点观察 Agent 抽象、异步事件、状态与恢复。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：加载和切分文档

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“加载和切分文档”推进为可检查成果；如果跳过，后续会缺少“Chunk 元数据完整”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“加载和切分文档”是这个核心能力的最小学习单元：你会通过“尽量复用第 6 周资料与切片规则；打印元数据确认两个框架语义一致”观察输入如何转化为“Chunk 元数据完整”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：尽量复用第 6 周资料与切片规则；打印元数据确认两个框架语义一致
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangChain Text Splitters](https://docs.langchain.com/oss/python/integrations/splitters)

**怎么验证学会了**

- 必交证据：Chunk 元数据完整。
- Teach-back：不看资料解释“加载和切分文档”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“加载和切分文档”后，应能观察到“Chunk 元数据完整”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangChain Text Splitters](https://docs.langchain.com/oss/python/integrations/splitters)
- 指定章节/条目：Retrieval → Building a knowledge base、Retrieval pipeline、Retriever as a tool
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：尽量复用第 6 周资料与切片规则；打印元数据确认两个框架语义一致
- 参考资料/文档产出：

- 主题：加载和切分文档
- 建议字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间
- 示例结论：已按统一口径产出“Chunk 元数据完整”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Chunk 元数据完整。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：复用 Qdrant 或建立实验索引

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“复用 Qdrant 或建立实验索引”推进为可检查成果；如果跳过，后续会缺少“完成语义检索”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“复用 Qdrant 或建立实验索引”是这个核心能力的最小学习单元：你会通过“为实验使用独立 Collection 或命名空间，避免覆盖 Spring AI Baseline”观察输入如何转化为“完成语义检索”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：为实验使用独立 Collection 或命名空间，避免覆盖 Spring AI Baseline
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Qdrant Collections](https://qdrant.tech/documentation/concepts/collections/)

**怎么验证学会了**

- 必交证据：完成语义检索。
- Teach-back：不看资料解释“复用 Qdrant 或建立实验索引”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“复用 Qdrant 或建立实验索引”后，应能观察到“完成语义检索”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Qdrant Collections](https://qdrant.tech/documentation/concepts/collections/)
- 指定章节/条目：Collections / Points → Vectors、Payload、Upsert points、Search
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：为实验使用独立 Collection 或命名空间，避免覆盖 Spring AI Baseline
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
from langchain.agents import create_agent
from langchain.tools import tool

@tool
def lookup(query: str) -> str:
    '''Search the fixed lesson dataset; query must be non-empty.'''
    if not query.strip():
        raise ValueError("EMPTY_QUERY")
    return "source=lesson-doc; answer=example"

agent = create_agent(model="provider:model", tools=[lookup])
result = agent.invoke({"messages": [{"role": "user", "content": "查找示例"}]})
print(result)
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成语义检索。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：将 Retriever 暴露为工具

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“将 Retriever 暴露为工具”推进为可检查成果；如果跳过，后续会缺少“Agent 自主判断是否检索”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“将 Retriever 暴露为工具”是这个核心能力的最小学习单元：你会通过“工具返回受控长度的片段与来源；准备无需检索的问题检查误调用”观察输入如何转化为“Agent 自主判断是否检索”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：工具返回受控长度的片段与来源；准备无需检索的问题检查误调用
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangChain Retrieval](https://docs.langchain.com/oss/python/langchain/retrieval)

**怎么验证学会了**

- 必交证据：Agent 自主判断是否检索。
- Teach-back：不看资料解释“将 Retriever 暴露为工具”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“将 Retriever 暴露为工具”后，应能观察到“Agent 自主判断是否检索”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangChain Retrieval](https://docs.langchain.com/oss/python/langchain/retrieval)
- 指定章节/条目：Retrieval → Building a knowledge base、Retrieval pipeline、Retriever as a tool
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：工具返回受控长度的片段与来源；准备无需检索的问题检查误调用
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
from langchain.agents import create_agent
from langchain.tools import tool

@tool
def lookup(query: str) -> str:
    '''Search the fixed lesson dataset; query must be non-empty.'''
    if not query.strip():
        raise ValueError("EMPTY_QUERY")
    return "source=lesson-doc; answer=example"

agent = create_agent(model="provider:model", tools=[lookup])
result = agent.invoke({"messages": [{"role": "user", "content": "查找示例"}]})
print(result)
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Agent 自主判断是否检索。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：增加受限文件工具

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“增加受限文件工具”推进为可检查成果；如果跳过，后续会缺少“保持只读和路径限制”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“增加受限文件工具”是这个核心能力的最小学习单元：你会通过“复用第 7 周安全用例，不因换成 Python 就降低路径校验标准”观察输入如何转化为“保持只读和路径限制”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：复用第 7 周安全用例，不因换成 Python 就降低路径校验标准
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OWASP Path Traversal](https://owasp.org/www-community/attacks/Path_Traversal)

**怎么验证学会了**

- 必交证据：保持只读和路径限制。
- Teach-back：不看资料解释“增加受限文件工具”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“增加受限文件工具”后，应能观察到“保持只读和路径限制”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OWASP Path Traversal](https://owasp.org/www-community/attacks/Path_Traversal)
- 指定章节/条目：该链接指向的“OWASP Path Traversal”整篇专题/章节；重点阅读与“增加受限文件工具”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：复用第 7 周安全用例，不因换成 Python 就降低路径校验标准
- 参考资料/文档产出：

- 主题：增加受限文件工具
- 建议字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令
- 示例结论：已按统一口径产出“保持只读和路径限制”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：保持只读和路径限制。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：返回结构化引用

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“返回结构化引用”推进为可检查成果；如果跳过，后续会缺少“回答关联来源”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“返回结构化引用”是这个核心能力的最小学习单元：你会通过“用 Pydantic 约束答案与引用；程序验证引用是否属于实际召回集合”观察输入如何转化为“回答关联来源”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：用 Pydantic 约束答案与引用；程序验证引用是否属于实际召回集合
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangChain Structured Output](https://docs.langchain.com/oss/python/langchain/structured-output)

**怎么验证学会了**

- 必交证据：回答关联来源。
- Teach-back：不看资料解释“返回结构化引用”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“返回结构化引用”后，应能观察到“回答关联来源”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangChain Structured Output](https://docs.langchain.com/oss/python/langchain/structured-output)
- 指定章节/条目：该链接指向的“LangChain Structured Output”整篇专题/章节；重点阅读与“返回结构化引用”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：用 Pydantic 约束答案与引用；程序验证引用是否属于实际召回集合
- 参考资料/文档产出：

- 主题：返回结构化引用
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“回答关联来源”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：回答关联来源。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：对比 Spring AI 与 LangChain

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“对比 Spring AI 与 LangChain”推进为可检查成果；如果跳过，后续会缺少“完成职责和 API 对比”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“对比 Spring AI 与 LangChain”是这个核心能力的最小学习单元：你会通过“用同一数据、问题、模型和指标；区分开发体验与回答质量”观察输入如何转化为“完成职责和 API 对比”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：用同一数据、问题、模型和指标；区分开发体验与回答质量
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangChain Retrieval](https://docs.langchain.com/oss/python/langchain/retrieval)

**怎么验证学会了**

- 必交证据：完成职责和 API 对比。
- Teach-back：不看资料解释“对比 Spring AI 与 LangChain”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“对比 Spring AI 与 LangChain”后，应能观察到“完成职责和 API 对比”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangChain Retrieval](https://docs.langchain.com/oss/python/langchain/retrieval)
- 指定章节/条目：该链接指向的“LangChain Retrieval”整篇专题/章节；重点阅读与“对比 Spring AI 与 LangChain”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：用同一数据、问题、模型和指标；区分开发体验与回答质量
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
from langchain.agents import create_agent
from langchain.tools import tool

@tool
def lookup(query: str) -> str:
    '''Search the fixed lesson dataset; query must be non-empty.'''
    if not query.strip():
        raise ValueError("EMPTY_QUERY")
    return "source=lesson-doc; answer=example"

agent = create_agent(model="provider:model", tools=[lookup])
result = agent.invoke({"messages": [{"role": "user", "content": "查找示例"}]})
print(result)
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：当样本、Owner 或数据来源不足时，把结论标为假设并给出验证计划；不能把模拟结果写成真实业务收益。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成职责和 API 对比。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：使用相同问题集验证

**为什么学**

本周要在相同数据和模型条件下建立第二套 RAG 实现，形成基于证据而非宣传的框架选择能力。本节把“使用相同问题集验证”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。今天的“使用相同问题集验证”是这个核心能力的最小学习单元：你会通过“保存完整成功与失败结果，不根据结果临时替换问题；总结选择建议”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：保存完整成功与失败结果，不根据结果临时替换问题；总结选择建议
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“使用相同问题集验证”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：LangChain RAG 把加载、切分、索引、Retriever、工具和结构化引用组合成检索增强 Agent；质量仍取决于数据与评测。
- 预测：完成“使用相同问题集验证”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)
- 指定章节/条目：该链接指向的“OpenAI Evals 指南”整篇专题/章节；重点阅读与“使用相同问题集验证”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：保存完整成功与失败结果，不根据结果临时替换问题；总结选择建议
- 参考资料/文档产出：

- 主题：使用相同问题集验证
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
