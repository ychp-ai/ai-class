<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 13 周教程：Claude 安全能力

> 计划来源：[04-claude-agent-sdk.md](../stages/04-claude-agent-sdk.md)。阶段文档决定课程任务和验收，[第 13 周公共成果要求](../../deliverables/week-13/README.md)定义门禁；学习状态只记录在个人学习仓库。

## 本周先理解

### 为什么学

把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。

### 这是什么

执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。

### 本周语言定位

必须使用 Python 3.11+、类型标注、Pydantic 和 pytest，重点观察 Agent 抽象、异步事件、状态与恢复。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：`allowed_tools`、`disallowed_tools` 和权限模式

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“`allowed_tools`、`disallowed_tools` 和权限模式”推进为可检查成果；如果跳过，后续会缺少“实现只读 Plan 模式”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“`allowed_tools`、`disallowed_tools` 和权限模式”是这个核心能力的最小学习单元：你会通过“从默认只读开始逐项增加权限；对每项权限写出任务理由和风险”观察输入如何转化为“实现只读 Plan 模式”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：从默认只读开始逐项增加权限；对每项权限写出任务理由和风险
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Permissions](https://code.claude.com/docs/en/agent-sdk/permissions)

**怎么验证学会了**

- 必交证据：实现只读 Plan 模式。
- Teach-back：不看资料解释“`allowed_tools`、`disallowed_tools` 和权限模式”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“`allowed_tools`、`disallowed_tools` 和权限模式”后，应能观察到“实现只读 Plan 模式”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Permissions](https://code.claude.com/docs/en/agent-sdk/permissions)
- 指定章节/条目：Permissions → Permission modes、allowedTools / disallowedTools、canUseTool
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：从默认只读开始逐项增加权限；对每项权限写出任务理由和风险
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：实现只读 Plan 模式。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：`canUseTool` 动态审批

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“`canUseTool` 动态审批”推进为可检查成果；如果跳过，后续会缺少“Edit 或 Bash 前可人工决定”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“`canUseTool` 动态审批”是这个核心能力的最小学习单元：你会通过“审批界面展示工具、完整目标、影响范围和原因；分别测试批准与拒绝”观察输入如何转化为“Edit 或 Bash 前可人工决定”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：审批界面展示工具、完整目标、影响范围和原因；分别测试批准与拒绝
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Permissions](https://code.claude.com/docs/en/agent-sdk/permissions)

**怎么验证学会了**

- 必交证据：Edit 或 Bash 前可人工决定。
- Teach-back：不看资料解释“`canUseTool` 动态审批”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“`canUseTool` 动态审批”后，应能观察到“Edit 或 Bash 前可人工决定”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Permissions](https://code.claude.com/docs/en/agent-sdk/permissions)
- 指定章节/条目：Interrupts → Pause using interrupt、Resuming interrupts、Approve or reject
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：审批界面展示工具、完整目标、影响范围和原因；分别测试批准与拒绝
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Edit 或 Bash 前可人工决定。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：PreToolUse Hook

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“PreToolUse Hook”推进为可检查成果；如果跳过，后续会缺少“拦截危险命令和敏感路径”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“PreToolUse Hook”是这个核心能力的最小学习单元：你会通过“采用显式规则匹配命令和规范化路径；用危险输入测试，不只测试正常命令”观察输入如何转化为“拦截危险命令和敏感路径”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：采用显式规则匹配命令和规范化路径；用危险输入测试，不只测试正常命令
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Hooks](https://code.claude.com/docs/en/agent-sdk/hooks)

**怎么验证学会了**

- 必交证据：拦截危险命令和敏感路径。
- Teach-back：不看资料解释“PreToolUse Hook”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“PreToolUse Hook”后，应能观察到“拦截危险命令和敏感路径”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Hooks](https://code.claude.com/docs/en/agent-sdk/hooks)
- 指定章节/条目：Hooks → Hook events、PreToolUse、PostToolUse、Hook input/output
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：采用显式规则匹配命令和规范化路径；用危险输入测试，不只测试正常命令
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：拦截危险命令和敏感路径。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：PostToolUse Hook

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“PostToolUse Hook”推进为可检查成果；如果跳过，后续会缺少“记录工具、参数、结果和耗时”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“PostToolUse Hook”是这个核心能力的最小学习单元：你会通过“记录关联 ID、工具名、状态、耗时和摘要；对参数与输出做脱敏和长度限制”观察输入如何转化为“记录工具、参数、结果和耗时”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：记录关联 ID、工具名、状态、耗时和摘要；对参数与输出做脱敏和长度限制
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Hooks](https://code.claude.com/docs/en/agent-sdk/hooks)

**怎么验证学会了**

- 必交证据：记录工具、参数、结果和耗时。
- Teach-back：不看资料解释“PostToolUse Hook”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“PostToolUse Hook”后，应能观察到“记录工具、参数、结果和耗时”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Hooks](https://code.claude.com/docs/en/agent-sdk/hooks)
- 指定章节/条目：Hooks → Hook events、PreToolUse、PostToolUse、Hook input/output
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：记录关联 ID、工具名、状态、耗时和摘要；对参数与输出做脱敏和长度限制
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：记录工具、参数、结果和耗时。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：接入自定义工具或 MCP

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“接入自定义工具或 MCP”推进为可检查成果；如果跳过，后续会缺少“使用一个外部只读能力”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“接入自定义工具或 MCP”是这个核心能力的最小学习单元：你会通过“选择天气、文档查询等只读工具；先独立调用验证 Schema，再开放给 Agent”观察输入如何转化为“使用一个外部只读能力”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：选择天气、文档查询等只读工具；先独立调用验证 Schema，再开放给 Agent
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK MCP](https://code.claude.com/docs/en/agent-sdk/mcp)

**怎么验证学会了**

- 必交证据：使用一个外部只读能力。
- Teach-back：不看资料解释“接入自定义工具或 MCP”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“接入自定义工具或 MCP”后，应能观察到“使用一个外部只读能力”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK MCP](https://code.claude.com/docs/en/agent-sdk/mcp)
- 指定章节/条目：MCP → Architecture / Tools / Authorization / Security best practices
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：选择天气、文档查询等只读工具；先独立调用验证 Schema，再开放给 Agent
- 参考资料/文档产出：

- 主题：接入自定义工具或 MCP
- 建议字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令
- 示例结论：已按统一口径产出“使用一个外部只读能力”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：使用一个外部只读能力。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：使用 Subagent 处理测试或审查

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“使用 Subagent 处理测试或审查”推进为可检查成果；如果跳过，后续会缺少“主 Agent 汇总子任务结果”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“使用 Subagent 处理测试或审查”是这个核心能力的最小学习单元：你会通过“选择可独立验收的子任务，限制上下文和权限；比较单 Agent 的成本与效果”观察输入如何转化为“主 Agent 汇总子任务结果”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：选择可独立验收的子任务，限制上下文和权限；比较单 Agent 的成本与效果
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Subagents](https://code.claude.com/docs/en/subagents)

**怎么验证学会了**

- 必交证据：主 Agent 汇总子任务结果。
- Teach-back：不看资料解释“使用 Subagent 处理测试或审查”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“使用 Subagent 处理测试或审查”后，应能观察到“主 Agent 汇总子任务结果”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Subagents](https://code.claude.com/docs/en/subagents)
- 指定章节/条目：该链接指向的“Claude Subagents”整篇专题/章节；重点阅读与“使用 Subagent 处理测试或审查”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：选择可独立验收的子任务，限制上下文和权限；比较单 Agent 的成本与效果
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
import asyncio
from claude_agent_sdk import ClaudeAgentOptions, query

async def main() -> None:
    options = ClaudeAgentOptions(
        allowed_tools=["Read", "Glob", "Grep"],
        cwd="sandbox-workspaces/demo",
    )
    async for message in query(prompt="分析项目并给出文件证据", options=options):
        print(type(message).__name__, message)

asyncio.run(main())
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：主 Agent 汇总子任务结果。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：设计沙箱、网络和凭证隔离

**为什么学**

本周要把最小权限、动态审批、Hooks、MCP 和沙箱落实到真实阻断测试，而不只写安全说明。本节把“设计沙箱、网络和凭证隔离”推进为可检查成果；如果跳过，后续会缺少“完成阶段提交”这项关键证据。

**这个是什么**

本周核心概念是：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。今天的“设计沙箱、网络和凭证隔离”是这个核心能力的最小学习单元：你会通过“画清宿主、容器、工作区、代理和凭证边界，运行至少一个真实阻断测试”观察输入如何转化为“完成阶段提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：画清宿主、容器、工作区、代理和凭证边界，运行至少一个真实阻断测试
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Claude Agent SDK Secure Deployment](https://code.claude.com/docs/en/agent-sdk/secure-deployment)

**怎么验证学会了**

- 必交证据：完成阶段提交。
- Teach-back：不看资料解释“设计沙箱、网络和凭证隔离”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：执行型 Agent 安全由权限集合、审批点、前后置 Hook、凭证与网络隔离共同组成；Prompt 不能充当安全边界。
- 预测：完成“设计沙箱、网络和凭证隔离”后，应能观察到“完成阶段提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Claude Agent SDK Secure Deployment](https://code.claude.com/docs/en/agent-sdk/secure-deployment)
- 指定章节/条目：该链接指向的“Claude Agent SDK Secure Deployment”整篇专题/章节；重点阅读与“设计沙箱、网络和凭证隔离”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：画清宿主、容器、工作区、代理和凭证边界，运行至少一个真实阻断测试
- 参考资料/文档产出：

- 主题：设计沙箱、网络和凭证隔离
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“完成阶段提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成阶段提交。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
