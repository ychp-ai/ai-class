<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 20 周教程：Tool、MCP 与知识库

> 计划来源：[06-ai-platform-core.md](../stages/06-ai-platform-core.md)。阶段文档决定课程任务和验收，[第 20 周公共成果要求](../../deliverables/week-20/README.md)定义门禁；学习状态只记录在个人学习仓库。

## 本周先理解

### 为什么学

把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。

### 这是什么

Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。

### 本周语言定位

Java 承担平台控制面与治理，Python 承担已授权的 Agent/Workflow 执行；TypeScript 只在管理流程需要时提供最小界面。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：Tool Registry 与版本

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“Tool Registry 与版本”推进为可检查成果；如果跳过，后续会缺少“工具注册和版本 API”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“Tool Registry 与版本”是这个核心能力的最小学习单元：你会通过“Schema、风险级别、Owner、权限和版本都必填；同名工具不可悄悄换语义”观察输入如何转化为“工具注册和版本 API”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：Schema、风险级别、Owner、权限和版本都必填；同名工具不可悄悄换语义
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[MCP Tools](https://modelcontextprotocol.io/specification/2025-11-25/server/tools)

**怎么验证学会了**

- 必交证据：工具注册和版本 API。
- Teach-back：不看资料解释“Tool Registry 与版本”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“Tool Registry 与版本”后，应能观察到“工具注册和版本 API”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[MCP Tools](https://modelcontextprotocol.io/specification/2025-11-25/server/tools)
- 指定章节/条目：该链接指向的“MCP Tools”整篇专题/章节；重点阅读与“Tool Registry 与版本”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：Schema、风险级别、Owner、权限和版本都必填；同名工具不可悄悄换语义
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“Tool Registry 与版本”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "工具注册和版本 API");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：工具注册和版本 API。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：Tool Credential 与权限

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“Tool Credential 与权限”推进为可检查成果；如果跳过，后续会缺少“无授权工具无法注入”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“Tool Credential 与权限”是这个核心能力的最小学习单元：你会通过“凭证使用引用，运行时按 Project、Agent、用户和 Scope 求交集”观察输入如何转化为“无授权工具无法注入”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：凭证使用引用，运行时按 Project、Agent、用户和 Scope 求交集
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[MCP Authorization](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization)

**怎么验证学会了**

- 必交证据：无授权工具无法注入。
- Teach-back：不看资料解释“Tool Credential 与权限”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“Tool Credential 与权限”后，应能观察到“无授权工具无法注入”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[MCP Authorization](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization)
- 指定章节/条目：该链接指向的“MCP Authorization”整篇专题/章节；重点阅读与“Tool Credential 与权限”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：凭证使用引用，运行时按 Project、Agent、用户和 Scope 求交集
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“Tool Credential 与权限”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "无授权工具无法注入");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：无授权工具无法注入。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：MCP Client/Server 与授权

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“MCP Client/Server 与授权”推进为可检查成果；如果跳过，后续会缺少“MCP 往返与拒绝案例”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“MCP Client/Server 与授权”是这个核心能力的最小学习单元：你会通过“同时做最小 Server 和 Client；HTTP 方式验证资源绑定，stdio 不套用 HTTP OAuth”观察输入如何转化为“MCP 往返与拒绝案例”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：同时做最小 Server 和 Client；HTTP 方式验证资源绑定，stdio 不套用 HTTP OAuth
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[MCP 2025-11-25 Specification](https://modelcontextprotocol.io/specification/2025-11-25)

**怎么验证学会了**

- 必交证据：MCP 往返与拒绝案例。
- Teach-back：不看资料解释“MCP Client/Server 与授权”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“MCP Client/Server 与授权”后，应能观察到“MCP 往返与拒绝案例”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[MCP 2025-11-25 Specification](https://modelcontextprotocol.io/specification/2025-11-25)
- 指定章节/条目：MCP → Architecture / Tools / Authorization / Security best practices
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：同时做最小 Server 和 Client；HTTP 方式验证资源绑定，stdio 不套用 HTTP OAuth
- 参考资料/文档产出：

- 主题：MCP Client/Server 与授权
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“MCP 往返与拒绝案例”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：MCP 往返与拒绝案例。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：动态工具发现与上下文预算

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“动态工具发现与上下文预算”推进为可检查成果；如果跳过，后续会缺少“Token 节省和质量对比”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“动态工具发现与上下文预算”是这个核心能力的最小学习单元：你会通过“工具多时先搜索再加载；对历史、工具结果和检索内容设置预算、裁剪和摘要策略”观察输入如何转化为“Token 节省和质量对比”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：工具多时先搜索再加载；对历史、工具结果和检索内容设置预算、裁剪和摘要策略
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangChain Context Engineering](https://docs.langchain.com/oss/python/langchain/context-engineering)

**怎么验证学会了**

- 必交证据：Token 节省和质量对比。
- Teach-back：不看资料解释“动态工具发现与上下文预算”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“动态工具发现与上下文预算”后，应能观察到“Token 节省和质量对比”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangChain Context Engineering](https://docs.langchain.com/oss/python/langchain/context-engineering)
- 指定章节/条目：该链接指向的“LangChain Context Engineering”整篇专题/章节；重点阅读与“动态工具发现与上下文预算”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：工具多时先搜索再加载；对历史、工具结果和检索内容设置预算、裁剪和摘要策略
- 参考资料/文档产出：

- 主题：动态工具发现与上下文预算
- 建议字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令
- 示例结论：已按统一口径产出“Token 节省和质量对比”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Token 节省和质量对比。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：Knowledge Base 控制面

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“Knowledge Base 控制面”推进为可检查成果；如果跳过，后续会缺少“索引版本可切换”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“Knowledge Base 控制面”是这个核心能力的最小学习单元：你会通过“管理数据源、Chunk、Embedding、索引版本和状态；重建索引不覆盖线上版本”观察输入如何转化为“索引版本可切换”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：管理数据源、Chunk、Embedding、索引版本和状态；重建索引不覆盖线上版本
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI ETL Pipeline](https://docs.spring.io/spring-ai/reference/api/etl-pipeline.html)

**怎么验证学会了**

- 必交证据：索引版本可切换。
- Teach-back：不看资料解释“Knowledge Base 控制面”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“Knowledge Base 控制面”后，应能观察到“索引版本可切换”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI ETL Pipeline](https://docs.spring.io/spring-ai/reference/api/etl-pipeline.html)
- 指定章节/条目：该链接指向的“Spring AI ETL Pipeline”整篇专题/章节；重点阅读与“Knowledge Base 控制面”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：管理数据源、Chunk、Embedding、索引版本和状态；重建索引不覆盖线上版本
- 参考资料/文档产出：

- 主题：Knowledge Base 控制面
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“索引版本可切换”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：索引版本可切换。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：进阶检索、权限与评测

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“进阶检索、权限与评测”推进为可检查成果；如果跳过，后续会缺少“质量提升且越权文档不召回”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“进阶检索、权限与评测”是这个核心能力的最小学习单元：你会通过“比较 BM25+向量混合检索、Rerank、Query Rewrite；加入 ACL，并用 Recall@K/MRR 验证”观察输入如何转化为“质量提升且越权文档不召回”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：比较 BM25+向量混合检索、Rerank、Query Rewrite；加入 ACL，并用 Recall@K/MRR 验证
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Qdrant Hybrid Queries](https://qdrant.tech/documentation/concepts/hybrid-queries/)

**怎么验证学会了**

- 必交证据：质量提升且越权文档不召回。
- Teach-back：不看资料解释“进阶检索、权限与评测”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“进阶检索、权限与评测”后，应能观察到“质量提升且越权文档不召回”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Qdrant Hybrid Queries](https://qdrant.tech/documentation/concepts/hybrid-queries/)
- 指定章节/条目：该链接指向的“Qdrant Hybrid Queries”整篇专题/章节；重点阅读与“进阶检索、权限与评测”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：比较 BM25+向量混合检索、Rerank、Query Rewrite；加入 ACL，并用 Recall@K/MRR 验证
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“进阶检索、权限与评测”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "质量提升且越权文档不召回");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：质量提升且越权文档不召回。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：平台核心端到端验收

**为什么学**

本周要把外部能力和资料变成带 Owner、版本、权限、预算和质量证据的平台资源。本节把“平台核心端到端验收”推进为可检查成果；如果跳过，后续会缺少“完成阶段提交”这项关键证据。

**这个是什么**

本周核心概念是：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。今天的“平台核心端到端验收”是这个核心能力的最小学习单元：你会通过“发布一个同时使用模型、工具和知识库的 Agent，记录每项依赖版本”观察输入如何转化为“完成阶段提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：发布一个同时使用模型、工具和知识库的 Agent，记录每项依赖版本
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[成果与提交标准](../06-deliverable-standards.md)

**怎么验证学会了**

- 必交证据：完成阶段提交。
- Teach-back：不看资料解释“平台核心端到端验收”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Tool Registry 管函数能力，MCP 标准化外部上下文和工具协议，Knowledge Base 控制数据源、索引版本、ACL 与检索配置。
- 预测：完成“平台核心端到端验收”后，应能观察到“完成阶段提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[成果与提交标准](../06-deliverable-standards.md)
- 指定章节/条目：该链接指向的“成果与提交标准”整篇专题/章节；重点阅读与“平台核心端到端验收”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：发布一个同时使用模型、工具和知识库的 Agent，记录每项依赖版本
- 参考资料/文档产出：

- 主题：平台核心端到端验收
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“完成阶段提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成阶段提交。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
