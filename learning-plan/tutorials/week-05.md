<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 05 周教程：Spring AI Chat

> 计划来源：[02-java-llm-rag-agent.md](../stages/02-java-llm-rag-agent.md)。阶段文档决定课程任务和验收，[第 05 周公共成果要求](../../deliverables/week-05/README.md)定义门禁；学习状态只记录在个人学习仓库。

## 本周先理解

### 为什么学

把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。

### 这是什么

Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。

### 本周语言定位

必须使用 Java 21、Maven、Spring Boot/Spring AI，并把前四周的原生能力映射到 Java 实现、测试和错误处理。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：初始化 Java 21 + Maven + Spring Boot

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“初始化 Java 21 + Maven + Spring Boot”推进为可检查成果；如果跳过，后续会缺少“健康检查可访问”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“初始化 Java 21 + Maven + Spring Boot”是这个核心能力的最小学习单元：你会通过“只引入 Web 和基础测试依赖，先保证空项目与健康检查稳定，再增加 AI 依赖”观察输入如何转化为“健康检查可访问”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：只引入 Web 和基础测试依赖，先保证空项目与健康检查稳定，再增加 AI 依赖
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring Boot Reference](https://docs.spring.io/spring-boot/reference/)

**怎么验证学会了**

- 必交证据：健康检查可访问。
- Teach-back：不看资料解释“初始化 Java 21 + Maven + Spring Boot”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“初始化 Java 21 + Maven + Spring Boot”后，应能观察到“健康检查可访问”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring Boot Reference](https://docs.spring.io/spring-boot/reference/)
- 指定章节/条目：Spring Boot Reference → Developing Your First Spring Boot Application；Actuator → Health endpoint
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：只引入 Web 和基础测试依赖，先保证空项目与健康检查稳定，再增加 AI 依赖
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
@SpringBootApplication
public class AiPlatformApplication {
    public static void main(String[] args) {
        SpringApplication.run(AiPlatformApplication.class, args);
    }
}

@RestController
final class HealthController {
    @GetMapping("/health")
    Map<String, String> health() { return Map.of("status", "UP"); }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：健康检查可访问。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：引入 Spring AI 模型 Starter

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“引入 Spring AI 模型 Starter”推进为可检查成果；如果跳过，后续会缺少“API Key 仅来自环境变量”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“引入 Spring AI 模型 Starter”是这个核心能力的最小学习单元：你会通过“使用官方 BOM 和当前文档；先配置 `.env.example`，再设置本地环境变量”观察输入如何转化为“API Key 仅来自环境变量”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：使用官方 BOM 和当前文档；先配置 `.env.example`，再设置本地环境变量
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI Getting Started](https://docs.spring.io/spring-ai/reference/getting-started.html)

**怎么验证学会了**

- 必交证据：API Key 仅来自环境变量。
- Teach-back：不看资料解释“引入 Spring AI 模型 Starter”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“引入 Spring AI 模型 Starter”后，应能观察到“API Key 仅来自环境变量”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI Getting Started](https://docs.spring.io/spring-ai/reference/getting-started.html)
- 指定章节/条目：Getting Started → Dependency Management、Add repositories and BOM、Model provider starter、API key properties
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：使用官方 BOM 和当前文档；先配置 `.env.example`，再设置本地环境变量
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```xml
<dependencyManagement>
  <dependencies>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-bom</artifactId>
      <version>${spring-ai.version}</version>
      <type>pom</type>
      <scope>import</scope>
    </dependency>
  </dependencies>
</dependencyManagement>
```

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
```

```dotenv
# .env.example：只写变量名，不写真实值
OPENAI_API_KEY=
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：API Key 仅来自环境变量。
- 完整字段：运行时与依赖版本；配置键；Secret 引用方式；示例占位值；启动命令；缺失配置的错误；泄露检查。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：实现 Chat API 或 CLI

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“实现 Chat API 或 CLI”推进为可检查成果；如果跳过，后续会缺少“完成一次真实模型调用”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“实现 Chat API 或 CLI”是这个核心能力的最小学习单元：你会通过“保持 Controller/CLI 薄，模型调用放在 Service；记录一次原始请求和响应结构”观察输入如何转化为“完成一次真实模型调用”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：保持 Controller/CLI 薄，模型调用放在 Service；记录一次原始请求和响应结构
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)

**怎么验证学会了**

- 必交证据：完成一次真实模型调用。
- Teach-back：不看资料解释“实现 Chat API 或 CLI”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“实现 Chat API 或 CLI”后，应能观察到“完成一次真实模型调用”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)
- 指定章节/条目：Chat Client API → Creating ChatClient、call()、stream()
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：保持 Controller/CLI 薄，模型调用放在 Service；记录一次原始请求和响应结构
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
@RestController
final class ChatController {
    private final ChatClient chatClient;

    ChatController(ChatClient.Builder builder) { this.chatClient = builder.build(); }

    @PostMapping("/api/chat")
    ChatResponse chat(@Valid @RequestBody ChatRequest request) {
        String content = chatClient.prompt().user(request.message()).call().content();
        return new ChatResponse(content == null ? "" : content);
    }
}
record ChatRequest(@NotBlank String message) {}
record ChatResponse(String content) {}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成一次真实模型调用。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：System Prompt 与结构化输出

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“System Prompt 与结构化输出”推进为可检查成果；如果跳过，后续会缺少“返回值能被 Java 对象解析”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“System Prompt 与结构化输出”是这个核心能力的最小学习单元：你会通过“先定义 Java DTO 和校验规则，再写 Prompt；主动测试字段缺失和类型错误”观察输入如何转化为“返回值能被 Java 对象解析”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：先定义 Java DTO 和校验规则，再写 Prompt；主动测试字段缺失和类型错误
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI Structured Output](https://docs.spring.io/spring-ai/reference/api/structured-output.html)

**怎么验证学会了**

- 必交证据：返回值能被 Java 对象解析。
- Teach-back：不看资料解释“System Prompt 与结构化输出”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“System Prompt 与结构化输出”后，应能观察到“返回值能被 Java 对象解析”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI Structured Output](https://docs.spring.io/spring-ai/reference/api/structured-output.html)
- 指定章节/条目：Structured Outputs → JSON Schema、Handling mistakes / Validation
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：先定义 Java DTO 和校验规则，再写 Prompt；主动测试字段缺失和类型错误
- 参考资料/文档产出：

- 主题：System Prompt 与结构化输出
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“返回值能被 Java 对象解析”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：返回值能被 Java 对象解析。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：实现流式响应

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“实现流式响应”推进为可检查成果；如果跳过，后续会缺少“客户端逐步接收内容”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“实现流式响应”是这个核心能力的最小学习单元：你会通过“用命令行客户端观察分块，关注断连、首 Token 延迟和流结束信号”观察输入如何转化为“客户端逐步接收内容”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：用命令行客户端观察分块，关注断连、首 Token 延迟和流结束信号
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)

**怎么验证学会了**

- 必交证据：客户端逐步接收内容。
- Teach-back：不看资料解释“实现流式响应”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“实现流式响应”后，应能观察到“客户端逐步接收内容”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI ChatClient](https://docs.spring.io/spring-ai/reference/api/chatclient.html)
- 指定章节/条目：Chat Client API → Creating ChatClient、call()、stream()
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：用命令行客户端观察分块，关注断连、首 Token 延迟和流结束信号
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
@RestController
final class ChatController {
    private final ChatClient chatClient;

    ChatController(ChatClient.Builder builder) { this.chatClient = builder.build(); }

    @PostMapping("/api/chat")
    ChatResponse chat(@Valid @RequestBody ChatRequest request) {
        String content = chatClient.prompt().user(request.message()).call().content();
        return new ChatResponse(content == null ? "" : content);
    }
}
record ChatRequest(@NotBlank String message) {}
record ChatResponse(String content) {}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：客户端逐步接收内容。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：处理超时、限流、空响应和模型错误

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“处理超时、限流、空响应和模型错误”推进为可检查成果；如果跳过，后续会缺少“有统一异常和日志”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“处理超时、限流、空响应和模型错误”是这个核心能力的最小学习单元：你会通过“使用 Stub 或错误配置稳定复现失败，不要等待真实服务偶然报错”观察输入如何转化为“有统一异常和日志”。它既包含可观察结果，也包含至少一个失败或不适用边界。重点边界来自课程原计划：不要等待真实服务偶然报错。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：使用 Stub 或错误配置稳定复现失败，不要等待真实服务偶然报错
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)

**怎么验证学会了**

- 必交证据：有统一异常和日志。
- Teach-back：不看资料解释“处理超时、限流、空响应和模型错误”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“处理超时、限流、空响应和模型错误”后，应能观察到“有统一异常和日志”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)
- 指定章节/条目：RFC 9333 → RateLimit-Limit、RateLimit-Remaining、RateLimit-Reset
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：使用 Stub 或错误配置稳定复现失败，不要等待真实服务偶然报错
- 参考资料/文档产出：

- 主题：处理超时、限流、空响应和模型错误
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“有统一异常和日志”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：有统一异常和日志。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：记录耗时、模型和 Token

**为什么学**

本周要把原生模型能力接入熟悉的 Java 服务，同时保留流式、配置、错误和观测边界。本节把“记录耗时、模型和 Token”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。今天的“记录耗时、模型和 Token”是这个核心能力的最小学习单元：你会通过“把运行、测试、配置和一次失败结果补进周成果，提交前检查密钥”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：把运行、测试、配置和一次失败结果补进周成果，提交前检查密钥
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Spring AI Observability](https://docs.spring.io/spring-ai/reference/observability/index.html)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“记录耗时、模型和 Token”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Spring AI Chat 是 Spring 生态中的模型与消息抽象；它减少 Provider 接入样板，但不替代业务权限、失败处理或质量验证。
- 预测：完成“记录耗时、模型和 Token”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Spring AI Observability](https://docs.spring.io/spring-ai/reference/observability/index.html)
- 指定章节/条目：Tokenizer summary → Tokenization pipeline、Encoding / Decoding
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：把运行、测试、配置和一次失败结果补进周成果，提交前检查密钥
- 参考资料/文档产出：

- 主题：记录耗时、模型和 Token
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
