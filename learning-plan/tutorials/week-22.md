<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 22 周教程：Durable Runtime

> 计划来源：[07-workflow-governance.md](../stages/07-workflow-governance.md)。阶段文档决定课程任务和验收，[第 22 周公共成果要求](../../deliverables/week-22/README.md)定义门禁；学习状态只记录在个人学习仓库。

## 本周先理解

### 为什么学

让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。

### 这是什么

Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。

### 本周语言定位

Java 承担平台控制面与治理，Python 承担已授权的 Agent/Workflow 执行；TypeScript 只在管理流程需要时提供最小界面。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：Run/Step/Event 状态机

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“Run/Step/Event 状态机”推进为可检查成果；如果跳过，后续会缺少“状态迁移单测”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“Run/Step/Event 状态机”是这个核心能力的最小学习单元：你会通过“明确 Pending、Running、Waiting、Succeeded、Failed、Cancelled；非法状态迁移必须拒绝”观察输入如何转化为“状态迁移单测”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：明确 Pending、Running、Waiting、Succeeded、Failed、Cancelled；非法状态迁移必须拒绝
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[W3C SCXML 状态机规范](https://www.w3.org/TR/scxml/)

**怎么验证学会了**

- 必交证据：状态迁移单测。
- Teach-back：不看资料解释“Run/Step/Event 状态机”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“Run/Step/Event 状态机”后，应能观察到“状态迁移单测”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[W3C SCXML 状态机规范](https://www.w3.org/TR/scxml/)
- 指定章节/条目：该链接指向的“W3C SCXML 状态机规范”整篇专题/章节；重点阅读与“Run/Step/Event 状态机”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：明确 Pending、Running、Waiting、Succeeded、Failed、Cancelled；非法状态迁移必须拒绝
- 参考资料/文档产出：

- 主题：Run/Step/Event 状态机
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“状态迁移单测”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：状态迁移单测。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：Checkpoint 和恢复

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“Checkpoint 和恢复”推进为可检查成果；如果跳过，后续会缺少“重启后不重复已完成步骤”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“Checkpoint 和恢复”是这个核心能力的最小学习单元：你会通过“每个有副作用节点前后保存边界；模拟进程退出后从上次成功点恢复”观察输入如何转化为“重启后不重复已完成步骤”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：每个有副作用节点前后保存边界；模拟进程退出后从上次成功点恢复
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)

**怎么验证学会了**

- 必交证据：重启后不重复已完成步骤。
- Teach-back：不看资料解释“Checkpoint 和恢复”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“Checkpoint 和恢复”后，应能观察到“重启后不重复已完成步骤”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)
- 指定章节/条目：Persistence → Threads、Checkpoints、Get state history、Replay
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：每个有副作用节点前后保存边界；模拟进程退出后从上次成功点恢复
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
from typing_extensions import TypedDict
from langgraph.graph import StateGraph, START, END

class State(TypedDict):
    task: str
    attempts: int

def validate(state: State) -> dict:
    if not state["task"].strip():
        raise ValueError("EMPTY_TASK")
    return {"attempts": state["attempts"] + 1}

graph = StateGraph(State)
graph.add_node("validate", validate)
graph.add_edge(START, "validate")
graph.add_edge("validate", END)
app = graph.compile()
print(app.invoke({"task": "demo", "attempts": 0}))
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：重启后不重复已完成步骤。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：幂等和副作用凭证

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“幂等和副作用凭证”推进为可检查成果；如果跳过，后续会缺少“重复投递不产生重复写入”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“幂等和副作用凭证”是这个核心能力的最小学习单元：你会通过“写工具使用 idempotency key；把请求、结果引用和副作用摘要绑定到 Step”观察输入如何转化为“重复投递不产生重复写入”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：写工具使用 idempotency key；把请求、结果引用和副作用摘要绑定到 Step
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[RFC 9110：幂等方法](https://www.rfc-editor.org/rfc/rfc9110.html#name-idempotent-methods)

**怎么验证学会了**

- 必交证据：重复投递不产生重复写入。
- Teach-back：不看资料解释“幂等和副作用凭证”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“幂等和副作用凭证”后，应能观察到“重复投递不产生重复写入”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[RFC 9110：幂等方法](https://www.rfc-editor.org/rfc/rfc9110.html#name-idempotent-methods)
- 指定章节/条目：RFC 9110 → 9.2.2 Idempotent Methods
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：写工具使用 idempotency key；把请求、结果引用和副作用摘要绑定到 Step
- 参考资料/文档产出：

- 主题：幂等和副作用凭证
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“重复投递不产生重复写入”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：重复投递不产生重复写入。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：异步任务、轮询与事件

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“异步任务、轮询与事件”推进为可检查成果；如果跳过，后续会缺少“提交、查询、事件链路可用”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“异步任务、轮询与事件”是这个核心能力的最小学习单元：你会通过“先实现清晰的异步协议和状态查询；消息队列只有在可靠性要求明确时引入”观察输入如何转化为“提交、查询、事件链路可用”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：先实现清晰的异步协议和状态查询；消息队列只有在可靠性要求明确时引入
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[RFC 7240：respond-async](https://www.rfc-editor.org/rfc/rfc7240.html)

**怎么验证学会了**

- 必交证据：提交、查询、事件链路可用。
- Teach-back：不看资料解释“异步任务、轮询与事件”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“异步任务、轮询与事件”后，应能观察到“提交、查询、事件链路可用”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[RFC 7240：respond-async](https://www.rfc-editor.org/rfc/rfc7240.html)
- 指定章节/条目：asyncio → Coroutines and Tasks、Streams、Running an asyncio program
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：先实现清晰的异步协议和状态查询；消息队列只有在可靠性要求明确时引入
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“异步任务、轮询与事件”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "提交、查询、事件链路可用");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：提交、查询、事件链路可用。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：Retry、Timeout 与补偿

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“Retry、Timeout 与补偿”推进为可检查成果；如果跳过，后续会缺少“故障矩阵与自动测试”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“Retry、Timeout 与补偿”是这个核心能力的最小学习单元：你会通过“节点级和全局截止时间分开；补偿不是简单重试，先列可逆和不可逆动作”观察输入如何转化为“故障矩阵与自动测试”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：节点级和全局截止时间分开；补偿不是简单重试，先列可逆和不可逆动作
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)

**怎么验证学会了**

- 必交证据：故障矩阵与自动测试。
- Teach-back：不看资料解释“Retry、Timeout 与补偿”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“Retry、Timeout 与补偿”后，应能观察到“故障矩阵与自动测试”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Resilience4j Guide](https://resilience4j.readme.io/docs/getting-started)
- 指定章节/条目：该链接指向的“Resilience4j Guide”整篇专题/章节；重点阅读与“Retry、Timeout 与补偿”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：节点级和全局截止时间分开；补偿不是简单重试，先列可逆和不可逆动作
- 参考资料/文档产出：

- 主题：Retry、Timeout 与补偿
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“故障矩阵与自动测试”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：故障矩阵与自动测试。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：取消、暂停和恢复

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“取消、暂停和恢复”推进为可检查成果；如果跳过，后续会缺少“四种状态行为可复现”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“取消、暂停和恢复”是这个核心能力的最小学习单元：你会通过“取消需传播到模型和工具；无法取消的外部动作要标为未知并人工核对”观察输入如何转化为“四种状态行为可复现”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：取消需传播到模型和工具；无法取消的外部动作要标为未知并人工核对
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangGraph Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)

**怎么验证学会了**

- 必交证据：四种状态行为可复现。
- Teach-back：不看资料解释“取消、暂停和恢复”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“取消、暂停和恢复”后，应能观察到“四种状态行为可复现”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangGraph Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)
- 指定章节/条目：Persistence → Threads、Checkpoints、Get state history、Replay
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：取消需传播到模型和工具；无法取消的外部动作要标为未知并人工核对
- 参考资料/文档产出：

- 主题：取消、暂停和恢复
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“四种状态行为可复现”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：四种状态行为可复现。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：故障注入演练

**为什么学**

本周要让长任务在进程退出、重复请求、取消和依赖故障下仍保持真实一致的状态。本节把“故障注入演练”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。今天的“故障注入演练”是这个核心能力的最小学习单元：你会通过“注入模型超时、工具 500、进程退出、重复事件和数据库短暂失败”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：注入模型超时、工具 500、进程退出、重复事件和数据库短暂失败
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Google SRE：Testing for Reliability](https://sre.google/sre-book/testing-reliability/)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“故障注入演练”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Durable Runtime 用 Run/Step 状态机、Checkpoint、幂等键和持久事件支持异步执行、恢复、取消与受控重试。
- 预测：完成“故障注入演练”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Google SRE：Testing for Reliability](https://sre.google/sre-book/testing-reliability/)
- 指定章节/条目：该链接指向的“Google SRE：Testing for Reliability”整篇专题/章节；重点阅读与“故障注入演练”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：注入模型超时、工具 500、进程退出、重复事件和数据库短暂失败
- 参考资料/文档产出：

- 主题：故障注入演练
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
