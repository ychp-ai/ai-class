<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 03 周教程：Prompt 与 AI 编程协作

> 计划来源：[01-ai-tools-and-engineering.md](../stages/01-ai-tools-and-engineering.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 03 周成果](../../deliverables/week-03/README.md)。

## 本周先理解

### 为什么学

把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。

### 这是什么

Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。

### 本周语言定位

默认使用 Python 3.11+，以较少样板观察 AI 原理和原生协议；记录虚拟环境、依赖版本、pytest 命令和失败结果。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：从函数契约理解 Prompt 的角色、上下文和约束

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“从函数契约理解 Prompt 的角色、上下文和约束”推进为可检查成果；如果跳过，后续会缺少“两版 Prompt 的输入、约束和输出差异有证据”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“从函数契约理解 Prompt 的角色、上下文和约束”是这个核心能力的最小学习单元：你会通过“类比函数的参数、返回类型、前置条件和错误约定，同时记录 Prompt 与确定性函数不一样的地方”观察输入如何转化为“两版 Prompt 的输入、约束和输出差异有证据”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：类比函数的参数、返回类型、前置条件和错误约定，同时记录 Prompt 与确定性函数不一样的地方
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)

**怎么验证学会了**

- 必交证据：两版 Prompt 的输入、约束和输出差异有证据。
- Teach-back：不看资料解释“从函数契约理解 Prompt 的角色、上下文和约束”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“从函数契约理解 Prompt 的角色、上下文和约束”后，应能观察到“两版 Prompt 的输入、约束和输出差异有证据”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)
- 指定章节/条目：Prompt engineering → Message roles、Few-shot learning、Provide reference text
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：类比函数的参数、返回类型、前置条件和错误约定，同时记录 Prompt 与确定性函数不一样的地方
- 参考资料/文档产出：

- 主题：从函数契约理解 Prompt 的角色、上下文和约束
- 建议字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明
- 示例结论：已按统一口径产出“两版 Prompt 的输入、约束和输出差异有证据”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：两版 Prompt 的输入、约束和输出差异有证据。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：Few-shot、反例和拒答

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“Few-shot、反例和拒答”推进为可检查成果；如果跳过，后续会缺少“固定样例集结果更稳定”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“Few-shot、反例和拒答”是这个核心能力的最小学习单元：你会通过“示例应覆盖边界而非只给正常路径；加入资料不足时明确拒答的例子”观察输入如何转化为“固定样例集结果更稳定”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：示例应覆盖边界而非只给正常路径；加入资料不足时明确拒答的例子
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)

**怎么验证学会了**

- 必交证据：固定样例集结果更稳定。
- Teach-back：不看资料解释“Few-shot、反例和拒答”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“Few-shot、反例和拒答”后，应能观察到“固定样例集结果更稳定”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)
- 指定章节/条目：Prompt engineering → Message roles、Few-shot learning、Provide reference text
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：示例应覆盖边界而非只给正常路径；加入资料不足时明确拒答的例子
- 参考资料/文档产出：

- 主题：Few-shot、反例和拒答
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“固定样例集结果更稳定”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：固定样例集结果更稳定。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：Prompt 版本与最小 Eval 回归集

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“Prompt 版本与最小 Eval 回归集”推进为可检查成果；如果跳过，后续会缺少“可重复比较两个 Prompt 版本”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“Prompt 版本与最小 Eval 回归集”是这个核心能力的最小学习单元：你会通过“先固定输入、预期、判定规则和模型配置；每次只改变一个主要变量，不为提分替换困难样例”观察输入如何转化为“可重复比较两个 Prompt 版本”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：先固定输入、预期、判定规则和模型配置；每次只改变一个主要变量，不为提分替换困难样例
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)

**怎么验证学会了**

- 必交证据：可重复比较两个 Prompt 版本。
- Teach-back：不看资料解释“Prompt 版本与最小 Eval 回归集”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“Prompt 版本与最小 Eval 回归集”后，应能观察到“可重复比较两个 Prompt 版本”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)
- 指定章节/条目：Prompt engineering → Message roles、Few-shot learning、Provide reference text
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：先固定输入、预期、判定规则和模型配置；每次只改变一个主要变量，不为提分替换困难样例
- 参考资料/文档产出：

- 主题：Prompt 版本与最小 Eval 回归集
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“可重复比较两个 Prompt 版本”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：可重复比较两个 Prompt 版本。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：AI 辅助理解代码仓库

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“AI 辅助理解代码仓库”推进为可检查成果；如果跳过，后续会缺少“形成可核验调用链说明”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“AI 辅助理解代码仓库”是这个核心能力的最小学习单元：你会通过“要求 AI 先搜索证据再下结论，所有结论回到文件和测试；抽查至少三项”观察输入如何转化为“形成可核验调用链说明”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：要求 AI 先搜索证据再下结论，所有结论回到文件和测试；抽查至少三项
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Google SRE：Effective Troubleshooting](https://sre.google/sre-book/effective-troubleshooting/)

**怎么验证学会了**

- 必交证据：形成可核验调用链说明。
- Teach-back：不看资料解释“AI 辅助理解代码仓库”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“AI 辅助理解代码仓库”后，应能观察到“形成可核验调用链说明”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Google SRE：Effective Troubleshooting](https://sre.google/sre-book/effective-troubleshooting/)
- 指定章节/条目：该链接指向的“Google SRE：Effective Troubleshooting”整篇专题/章节；重点阅读与“AI 辅助理解代码仓库”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：要求 AI 先搜索证据再下结论，所有结论回到文件和测试；抽查至少三项
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
def run_case(input_data: dict) -> dict:
    if not input_data:
        return {"status": "FAILED", "error": "EMPTY_INPUT"}
    # Replace this line with the lesson-specific call.
    result = {"observed": input_data, "status": "SUCCEEDED"}
    return result

print(run_case({"case": "normal"}))
print(run_case({}))  # failure case
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：形成可核验调用链说明。
- 完整字段：目标；事实与证据；关键决定；备选方案；失败与风险；限制；后续动作；负责人/时间。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：AI 辅助实现最小变更

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“AI 辅助实现最小变更”推进为可检查成果；如果跳过，后续会缺少“一个小功能测试通过”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“AI 辅助实现最小变更”是这个核心能力的最小学习单元：你会通过“先写验收测试再让 AI 修改；检查 Diff 中的额外重构和依赖变化”观察输入如何转化为“一个小功能测试通过”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：先写验收测试再让 AI 修改；检查 Diff 中的额外重构和依赖变化
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Martin Fowler：Test Pyramid](https://martinfowler.com/articles/practical-test-pyramid.html)

**怎么验证学会了**

- 必交证据：一个小功能测试通过。
- Teach-back：不看资料解释“AI 辅助实现最小变更”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“AI 辅助实现最小变更”后，应能观察到“一个小功能测试通过”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Martin Fowler：Test Pyramid](https://martinfowler.com/articles/practical-test-pyramid.html)
- 指定章节/条目：该链接指向的“Martin Fowler：Test Pyramid”整篇专题/章节；重点阅读与“AI 辅助实现最小变更”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：先写验收测试再让 AI 修改；检查 Diff 中的额外重构和依赖变化
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```python
def run_case(input_data: dict) -> dict:
    if not input_data:
        return {"status": "FAILED", "error": "EMPTY_INPUT"}
    # Replace this line with the lesson-specific call.
    result = {"observed": input_data, "status": "SUCCEEDED"}
    return result

print(run_case({"case": "normal"}))
print(run_case({}))  # failure case
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：一个小功能测试通过。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：AI 输出审查与安全

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“AI 输出审查与安全”推进为可检查成果；如果跳过，后续会缺少“审查清单和拒绝记录”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“AI 输出审查与安全”是这个核心能力的最小学习单元：你会通过“检查命令、依赖、许可证、敏感信息和越权访问；至少拒绝一条不安全建议”观察输入如何转化为“审查清单和拒绝记录”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：检查命令、依赖、许可证、敏感信息和越权访问；至少拒绝一条不安全建议
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OWASP Agentic AI Top 10](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/)

**怎么验证学会了**

- 必交证据：审查清单和拒绝记录。
- Teach-back：不看资料解释“AI 输出审查与安全”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“AI 输出审查与安全”后，应能观察到“审查清单和拒绝记录”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OWASP Agentic AI Top 10](https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/)
- 指定章节/条目：该链接指向的“OWASP Agentic AI Top 10”整篇专题/章节；重点阅读与“AI 输出审查与安全”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：检查命令、依赖、许可证、敏感信息和越权访问；至少拒绝一条不安全建议
- 参考资料/文档产出：

- 主题：AI 输出审查与安全
- 建议字段：对象；Owner/角色；输入或来源；规则/约束；风险；证据；状态；下一步
- 示例结论：已按统一口径产出“审查清单和拒绝记录”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：审查清单和拒绝记录。
- 完整字段：对象；Owner/角色；输入或来源；规则/约束；风险；证据；状态；下一步。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：固化可复用协作模板

**为什么学**

本周要把 Prompt 和 AI 生成代码纳入版本、测试与审查，避免一次对话看似成功却无法复现和回归。本节把“固化可复用协作模板”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。今天的“固化可复用协作模板”是这个核心能力的最小学习单元：你会通过“模板包含目标、上下文、非目标、验收、可用工具和停止条件”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：模板包含目标、上下文、非目标、验收、可用工具和停止条件
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[成果与提交标准](../06-deliverable-standards.md)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“固化可复用协作模板”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：Prompt 是可版本化、可评测的模型输入契约；AI 编程协作是以 Diff、测试和人工审查约束生成式助手，而不是把代码责任交给模型。
- 预测：完成“固化可复用协作模板”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[成果与提交标准](../06-deliverable-standards.md)
- 指定章节/条目：该链接指向的“成果与提交标准”整篇专题/章节；重点阅读与“固化可复用协作模板”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：模板包含目标、上下文、非目标、验收、可用工具和停止条件
- 参考资料/文档产出：

- 主题：固化可复用协作模板
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
