<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 23 周教程：Governance 与 Security

> 计划来源：[07-workflow-governance.md](../stages/07-workflow-governance.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 23 周成果](../../deliverables/week-23/README.md)。

## 本周先理解

### 为什么学

把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。

### 这是什么

平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：RBAC 与资源权限

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“RBAC 与资源权限”推进为可检查成果；如果跳过，后续会缺少“角色权限矩阵和测试”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“RBAC 与资源权限”是这个核心能力的最小学习单元：你会通过“权限至少覆盖查看、编辑、发布、运行、审批和审计；默认拒绝且查询带资源归属”观察输入如何转化为“角色权限矩阵和测试”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：权限至少覆盖查看、编辑、发布、运行、审批和审计；默认拒绝且查询带资源归属
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[NIST RBAC Model](https://csrc.nist.gov/projects/role-based-access-control)

**怎么验证学会了**

- 必交证据：角色权限矩阵和测试。
- Teach-back：不看资料解释“RBAC 与资源权限”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“RBAC 与资源权限”后，应能观察到“角色权限矩阵和测试”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[NIST RBAC Model](https://csrc.nist.gov/projects/role-based-access-control)
- 指定章节/条目：该链接指向的“NIST RBAC Model”整篇专题/章节；重点阅读与“RBAC 与资源权限”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：权限至少覆盖查看、编辑、发布、运行、审批和审计；默认拒绝且查询带资源归属
- 参考资料/文档产出：

- 主题：RBAC 与资源权限
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“角色权限矩阵和测试”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：角色权限矩阵和测试。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：审批策略与任务内容

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“审批策略与任务内容”推进为可检查成果；如果跳过，后续会缺少“三种决策可恢复工作流”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“审批策略与任务内容”是这个核心能力的最小学习单元：你会通过“审批卡片展示目标、参数、影响、成本和验收；批准、编辑、拒绝都留记录”观察输入如何转化为“三种决策可恢复工作流”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：审批卡片展示目标、参数、影响、成本和验收；批准、编辑、拒绝都留记录
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[LangGraph Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)

**怎么验证学会了**

- 必交证据：三种决策可恢复工作流。
- Teach-back：不看资料解释“审批策略与任务内容”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“审批策略与任务内容”后，应能观察到“三种决策可恢复工作流”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[LangGraph Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)
- 指定章节/条目：Interrupts → Pause using interrupt、Resuming interrupts、Approve or reject
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：审批卡片展示目标、参数、影响、成本和验收；批准、编辑、拒绝都留记录
- 参考资料/文档产出：

- 主题：审批策略与任务内容
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“三种决策可恢复工作流”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：三种决策可恢复工作流。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：审批超时与职责分离

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“审批超时与职责分离”推进为可检查成果；如果跳过，后续会缺少“自批和超时被阻止”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“审批超时与职责分离”是这个核心能力的最小学习单元：你会通过“高风险操作不能由发起人自批；超时应转人工或失败，不默认通过”观察输入如何转化为“自批和超时被阻止”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：高风险操作不能由发起人自批；超时应转人工或失败，不默认通过
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[NIST RBAC Model](https://csrc.nist.gov/projects/role-based-access-control)

**怎么验证学会了**

- 必交证据：自批和超时被阻止。
- Teach-back：不看资料解释“审批超时与职责分离”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“审批超时与职责分离”后，应能观察到“自批和超时被阻止”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[NIST RBAC Model](https://csrc.nist.gov/projects/role-based-access-control)
- 指定章节/条目：Interrupts → Pause using interrupt、Resuming interrupts、Approve or reject
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：高风险操作不能由发起人自批；超时应转人工或失败，不默认通过
- 参考资料/文档产出：

- 主题：审批超时与职责分离
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“自批和超时被阻止”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：自批和超时被阻止。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：审计日志与证据链

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“审计日志与证据链”推进为可检查成果；如果跳过，后续会缺少“版本发布到工具调用可追溯”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“审计日志与证据链”是这个核心能力的最小学习单元：你会通过“记录谁在何时用哪个版本执行什么，敏感参数使用摘要或 Hash”观察输入如何转化为“版本发布到工具调用可追溯”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：记录谁在何时用哪个版本执行什么，敏感参数使用摘要或 Hash
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OWASP Logging Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html)

**怎么验证学会了**

- 必交证据：版本发布到工具调用可追溯。
- Teach-back：不看资料解释“审计日志与证据链”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“审计日志与证据链”后，应能观察到“版本发布到工具调用可追溯”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OWASP Logging Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html)
- 指定章节/条目：Trace/Log semantic conventions → Span、Attributes、Context propagation
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：记录谁在何时用哪个版本执行什么，敏感参数使用摘要或 Hash
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“审计日志与证据链”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "版本发布到工具调用可追溯");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：版本发布到工具调用可追溯。
- 完整字段：入口；输入类型；输出类型；依赖版本；校验与权限；超时/错误；日志字段；最小测试命令。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：Prompt Injection 攻击实验

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“Prompt Injection 攻击实验”推进为可检查成果；如果跳过，后续会缺少“直接/间接注入阻断记录”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“Prompt Injection 攻击实验”是这个核心能力的最小学习单元：你会通过“把网页、RAG 文档和工具输出视为不可信数据；测试指令覆盖和数据外泄”观察输入如何转化为“直接/间接注入阻断记录”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：把网页、RAG 文档和工具输出视为不可信数据；测试指令覆盖和数据外泄
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OWASP Prompt Injection](https://genai.owasp.org/llmrisk/llm01-prompt-injection/)

**怎么验证学会了**

- 必交证据：直接/间接注入阻断记录。
- Teach-back：不看资料解释“Prompt Injection 攻击实验”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“Prompt Injection 攻击实验”后，应能观察到“直接/间接注入阻断记录”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OWASP Prompt Injection](https://genai.owasp.org/llmrisk/llm01-prompt-injection/)
- 指定章节/条目：Prompt engineering → Message roles、Few-shot learning、Provide reference text
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：把网页、RAG 文档和工具输出视为不可信数据；测试指令覆盖和数据外泄
- 参考资料/文档产出：

- 主题：Prompt Injection 攻击实验
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“直接/间接注入阻断记录”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：直接/间接注入阻断记录。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：MCP/Tool 投毒与输出校验

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“MCP/Tool 投毒与输出校验”推进为可检查成果；如果跳过，后续会缺少“至少四类攻击测试”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“MCP/Tool 投毒与输出校验”是这个核心能力的最小学习单元：你会通过“测试伪造工具描述、过宽 Scope、恶意输出和 Token 透传；执行前验证结构和权限”观察输入如何转化为“至少四类攻击测试”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：测试伪造工具描述、过宽 Scope、恶意输出和 Token 透传；执行前验证结构和权限
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[MCP Security Best Practices](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices)

**怎么验证学会了**

- 必交证据：至少四类攻击测试。
- Teach-back：不看资料解释“MCP/Tool 投毒与输出校验”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“MCP/Tool 投毒与输出校验”后，应能观察到“至少四类攻击测试”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[MCP Security Best Practices](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices)
- 指定章节/条目：MCP → Architecture / Tools / Authorization / Security best practices
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：测试伪造工具描述、过宽 Scope、恶意输出和 Token 透传；执行前验证结构和权限
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“MCP/Tool 投毒与输出校验”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "至少四类攻击测试");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：至少四类攻击测试。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：威胁模型与残余风险

**为什么学**

本周要把谁能做什么、何时审批、发生过什么和攻击如何阻断落实到执行路径。本节把“威胁模型与残余风险”推进为可检查成果；如果跳过，后续会缺少“完成本周提交”这项关键证据。

**这个是什么**

本周核心概念是：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。今天的“威胁模型与残余风险”是这个核心能力的最小学习单元：你会通过“按资产、主体、信任边界、攻击路径、防护和残余风险整理”观察输入如何转化为“完成本周提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：按资产、主体、信任边界、攻击路径、防护和残余风险整理
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OWASP Threat Modeling](https://owasp.org/www-community/Threat_Modeling)

**怎么验证学会了**

- 必交证据：完成本周提交。
- Teach-back：不看资料解释“威胁模型与残余风险”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台治理由 RBAC、策略、职责分离、审批、审计和攻击测试构成；前端隐藏按钮和 Prompt 约束都不是授权。
- 预测：完成“威胁模型与残余风险”后，应能观察到“完成本周提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OWASP Threat Modeling](https://owasp.org/www-community/Threat_Modeling)
- 指定章节/条目：该链接指向的“OWASP Threat Modeling”整篇专题/章节；重点阅读与“威胁模型与残余风险”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：按资产、主体、信任边界、攻击路径、防护和残余风险整理
- 参考资料/文档产出：

- 主题：威胁模型与残余风险
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“完成本周提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成本周提交。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
