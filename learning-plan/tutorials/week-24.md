<!-- 本文件由 scripts/generate_course_tutorials.py 生成，请修改阶段计划或生成器后重新生成。 -->

# 第 24 周教程：Eval 与 Operations

> 计划来源：[07-workflow-governance.md](../stages/07-workflow-governance.md)。阶段文档决定课程任务和验收，本文件解释逐节学习方法；学习状态仍只记录在[第 24 周成果](../../deliverables/week-24/README.md)。

## 本周先理解

### 为什么学

把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。

### 这是什么

平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。

### 本周语言定位

Java 承担平台控制面与治理，Python 承担已授权的 Agent/Workflow 执行；TypeScript 只在管理流程需要时提供最小界面。完整职责、切换桥接和替代规则见[开发语言路线](../01-language-roadmap.md)。

### 本周学习策略

- 每天只完成下面一节，控制在约 1 小时，不提前堆叠下一节的新概念。
- 先预测再实验；先保存原始证据，再写结论；失败样例与正常结果同等重要。
- 首次遇到术语时补齐：一句话定义、输入输出、开发类比、类比边界、反例和“不是什么”。
- 使用 H1→H4 提示阶梯；若使用完整参考答案，必须额外完成一个不同输入或约束的变体。

## 逐节教程

### 周一 · 第 1 节：平台 Eval 对象模型

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“平台 Eval 对象模型”推进为可检查成果；如果跳过，后续会缺少“Eval Schema 和 API”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“平台 Eval 对象模型”是这个核心能力的最小学习单元：你会通过“Dataset、Case、Evaluator、Run、Score 与 Agent/Workflow Version 关联；数据集发布后不可偷改”观察输入如何转化为“Eval Schema 和 API”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：Dataset、Case、Evaluator、Run、Score 与 Agent/Workflow Version 关联；数据集发布后不可偷改
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)

**怎么验证学会了**

- 必交证据：Eval Schema 和 API。
- Teach-back：不看资料解释“平台 Eval 对象模型”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“平台 Eval 对象模型”后，应能观察到“Eval Schema 和 API”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Evals 指南](https://platform.openai.com/docs/guides/evals)
- 指定章节/条目：该链接指向的“OpenAI Evals 指南”整篇专题/章节；重点阅读与“平台 Eval 对象模型”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：Dataset、Case、Evaluator、Run、Score 与 Agent/Workflow Version 关联；数据集发布后不可偷改
- 样例代码（先核对课程链接中的当前 API，再按本仓库边界修改）：

```java
record LessonCommand(String projectId, String requestId, String payload) {}
record LessonResult(String status, String evidence) {}

@Service
final class LessonService {
    LessonResult execute(LessonCommand command) {
        if (command.projectId() == null || command.projectId().isBlank()) {
            throw new IllegalArgumentException("PROJECT_REQUIRED");
        }
        // TODO: 实现“平台 Eval 对象模型”，并在执行路径校验权限、版本和幂等约束。
        return new LessonResult("SUCCEEDED", "Eval Schema 和 API");
    }
}
```

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：Eval Schema 和 API。
- 完整字段：版本；请求字段；成功响应；稳定错误码；状态变化；traceId；幂等/并发约束；兼容性说明。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周二 · 第 2 节：离线回归和发布门禁

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“离线回归和发布门禁”推进为可检查成果；如果跳过，后续会缺少“劣化版本被阻止”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“离线回归和发布门禁”是这个核心能力的最小学习单元：你会通过“设置任务成功、安全、延迟和成本阈值；门禁失败不允许发布但可保留结果”观察输入如何转化为“劣化版本被阻止”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：设置任务成功、安全、延迟和成本阈值；门禁失败不允许发布但可保留结果
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)

**怎么验证学会了**

- 必交证据：劣化版本被阻止。
- Teach-back：不看资料解释“离线回归和发布门禁”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“离线回归和发布门禁”后，应能观察到“劣化版本被阻止”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenAI Eval 最佳实践](https://platform.openai.com/docs/guides/evaluation-best-practices)
- 指定章节/条目：该链接指向的“OpenAI Eval 最佳实践”整篇专题/章节；重点阅读与“离线回归和发布门禁”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：设置任务成功、安全、延迟和成本阈值；门禁失败不允许发布但可保留结果
- 参考资料/文档产出：

- 主题：离线回归和发布门禁
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“劣化版本被阻止”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：劣化版本被阻止。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周三 · 第 3 节：Trace 与统一观测字段

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“Trace 与统一观测字段”推进为可检查成果；如果跳过，后续会缺少“跨服务 Trace 可查询”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“Trace 与统一观测字段”是这个核心能力的最小学习单元：你会通过“traceId 贯穿 Java、Python、模型和 Tool；默认不记录敏感 Prompt/结果正文”观察输入如何转化为“跨服务 Trace 可查询”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：traceId 贯穿 Java、Python、模型和 Tool；默认不记录敏感 Prompt/结果正文
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/specs/semconv/)

**怎么验证学会了**

- 必交证据：跨服务 Trace 可查询。
- Teach-back：不看资料解释“Trace 与统一观测字段”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“Trace 与统一观测字段”后，应能观察到“跨服务 Trace 可查询”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[OpenTelemetry Semantic Conventions](https://opentelemetry.io/docs/specs/semconv/)
- 指定章节/条目：Trace/Log semantic conventions → Span、Attributes、Context propagation
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：traceId 贯穿 Java、Python、模型和 Tool；默认不记录敏感 Prompt/结果正文
- 参考资料/文档产出：

- 主题：Trace 与统一观测字段
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“跨服务 Trace 可查询”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：跨服务 Trace 可查询。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周四 · 第 4 节：指标、SLO 和告警

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“指标、SLO 和告警”推进为可检查成果；如果跳过，后续会缺少“SLO 与告警规则草案”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“指标、SLO 和告警”是这个核心能力的最小学习单元：你会通过“选择可操作指标：成功率、P95、等待审批时长、Token、成本和越权阻断”观察输入如何转化为“SLO 与告警规则草案”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：选择可操作指标：成功率、P95、等待审批时长、Token、成本和越权阻断
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Google SRE：Service Level Objectives](https://sre.google/sre-book/service-level-objectives/)

**怎么验证学会了**

- 必交证据：SLO 与告警规则草案。
- Teach-back：不看资料解释“指标、SLO 和告警”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“指标、SLO 和告警”后，应能观察到“SLO 与告警规则草案”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Google SRE：Service Level Objectives](https://sre.google/sre-book/service-level-objectives/)
- 指定章节/条目：Service Level Objectives → SLI、SLO、Error budgets
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：选择可操作指标：成功率、P95、等待审批时长、Token、成本和越权阻断
- 参考资料/文档产出：

- 主题：指标、SLO 和告警
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“SLO 与告警规则草案”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：SLO 与告警规则草案。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周五 · 第 5 节：限流、背压和容量测试

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“限流、背压和容量测试”推进为可检查成果；如果跳过，后续会缺少“压测曲线和容量结论”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“限流、背压和容量测试”是这个核心能力的最小学习单元：你会通过“分别压模型、工作流和工具；记录饱和点和降级行为，不追求虚高 QPS”观察输入如何转化为“压测曲线和容量结论”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：分别压模型、工作流和工具；记录饱和点和降级行为，不追求虚高 QPS
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[Grafana k6 Documentation](https://grafana.com/docs/k6/latest/)

**怎么验证学会了**

- 必交证据：压测曲线和容量结论。
- Teach-back：不看资料解释“限流、背压和容量测试”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“限流、背压和容量测试”后，应能观察到“压测曲线和容量结论”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[Grafana k6 Documentation](https://grafana.com/docs/k6/latest/)
- 指定章节/条目：RFC 9333 → RateLimit-Limit、RateLimit-Remaining、RateLimit-Reset
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：分别压模型、工作流和工具；记录饱和点和降级行为，不追求虚高 QPS
- 参考资料/文档产出：

- 主题：限流、背压和容量测试
- 建议字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项
- 示例结论：已按统一口径产出“压测曲线和容量结论”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：加入无答案、非法 Schema、超时或工具失败输入，正确结果应是拒答或稳定失败状态；如果输出看似正常但证据缺失，应判为失败。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：压测曲线和容量结论。
- 完整字段：指标名称；计算公式；数据来源；样本与时间窗；当前值；目标/阈值；限制与未验证项。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周六 · 第 6 节：备份恢复与故障手册

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“备份恢复与故障手册”推进为可检查成果；如果跳过，后续会缺少“恢复演练证据”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“备份恢复与故障手册”是这个核心能力的最小学习单元：你会通过“备份元数据、Checkpoint、评测和索引定义；实际恢复一次而非只写文档”观察输入如何转化为“恢复演练证据”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：备份元数据、Checkpoint、评测和索引定义；实际恢复一次而非只写文档
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[PostgreSQL Backup and Restore](https://www.postgresql.org/docs/current/backup.html)

**怎么验证学会了**

- 必交证据：恢复演练证据。
- Teach-back：不看资料解释“备份恢复与故障手册”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“备份恢复与故障手册”后，应能观察到“恢复演练证据”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[PostgreSQL Backup and Restore](https://www.postgresql.org/docs/current/backup.html)
- 指定章节/条目：Persistence → Threads、Checkpoints、Get state history、Replay
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：备份元数据、Checkpoint、评测和索引定义；实际恢复一次而非只写文档
- 参考资料/文档产出：

- 主题：备份恢复与故障手册
- 建议字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过
- 示例结论：已按统一口径产出“恢复演练证据”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删掉一个必填输入或让依赖失败，系统应返回可解释的失败结果且不产生假成功；再根据日志、Diff 或流程证据定位原因。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：恢复演练证据。
- 完整字段：用例 ID；前置条件；输入/故障；预期状态；关键断言；实际结果；日志或 Trace；是否通过。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


### 周日 · 第 7 节：平台阶段验收

**为什么学**

本周要把质量、安全、成本、延迟和恢复能力变成发布门禁与日常运行标准。本节把“平台阶段验收”推进为可检查成果；如果跳过，后续会缺少“完成阶段提交”这项关键证据。

**这个是什么**

本周核心概念是：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。今天的“平台阶段验收”是这个核心能力的最小学习单元：你会通过“从 Agent Draft 到发布、运行、审批、观测、评测和回滚完整演示”观察输入如何转化为“完成阶段提交”。它既包含可观察结果，也包含至少一个失败或不适用边界。只读完资料或只跑通正常路径，都不能证明已经掌握。

**怎么学（约 60 分钟）**

1. `0–5 分钟`：不查资料，写下你对本节主题的一句话解释、预期输入输出和一个疑问。
2. `5–15 分钟`：只阅读下方资料中与当前任务直接相关的部分，记录一个关键约束和版本信息。
3. `15–45 分钟`：执行专项任务：从 Agent Draft 到发布、运行、审批、观测、评测和回滚完整演示
4. `45–55 分钟`：主动制造一个错误输入、边界条件、依赖失败或反例，保存现象并解释原因。
5. `55–60 分钟`：整理命令、输入、输出、Diff、图表或访谈证据，并用自己的语言完成 Teach-back。

专项资料：[成果与提交标准](../06-deliverable-standards.md)

**怎么验证学会了**

- 必交证据：完成阶段提交。
- Teach-back：不看资料解释“平台阶段验收”解决什么问题、主要输入输出、一个边界，以及它不是什么。
- 失败证据：展示一个非正常场景，指出失败发生在哪一层，不能只贴错误截图。
- 变体任务：改变一个输入、参数、角色、权限或失败条件，在不照抄原步骤的情况下重新完成。
- 通过标准：以上四项均有证据，且能解释关键取舍；只能照步骤运行时最高算 L1，不能判定本节通过。

**参考答案（独立尝试至少 20 分钟后再看）**

> 这是一份合格答案骨架，不是唯一实现。看完后必须关闭参考答案，换一个输入或约束独立重做。

#### 步骤 1 参考：先写预测

- 一句话解释：平台运维用 Eval Dataset、SLO、Trace、告警、压测、备份恢复和 Runbook 管理发布后的可用性与风险。
- 预测：完成“平台阶段验收”后，应能观察到“完成阶段提交”；失败输入不应产生假成功。

#### 步骤 2 参考：阅读资料

- 链接：[成果与提交标准](../06-deliverable-standards.md)
- 指定章节/条目：该链接指向的“成果与提交标准”整篇专题/章节；重点阅读与“平台阶段验收”直接对应的段落，页面更新时使用课程标题中的英文术语或 API 名页内搜索
- 阅读方法：只摘录一个定义、一个输入输出约束和一个失败边界；记录页面日期、协议版本或依赖版本。

#### 步骤 3 参考：按专项动作完成产出

- 专项动作：从 Agent Draft 到发布、运行、审批、观测、评测和回滚完整演示
- 参考资料/文档产出：

- 主题：平台阶段验收
- 建议字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置
- 示例结论：已按统一口径产出“完成阶段提交”；证据不足的部分标记为假设或未验证。

#### 步骤 4 参考：制造失败并解释

- 失败注入：删除身份、Scope 或审批后再次执行，正确结果应是执行路径明确拒绝且留下脱敏审计；如果仍成功，就是权限边界失效。
- 参考判断：失败必须映射为明确状态、错误、拒答、人工路径或未验证假设；日志和界面不得显示成功。

#### 步骤 5 参考：整理交付与 Teach-back

- 当天交付：完成阶段提交。
- 完整字段：一句话定义；输入；操作；可观察输出；失败/反例；工程意义；证据位置。
- Teach-back 参考：本节输入是什么、经过了什么处理、输出如何验证、哪种失败最危险、为什么当前方案没有越过课程边界。
- 完成后变体：更换一个输入、参数、角色、权限或失败条件，不看上述样例重新完成。


## 本周收尾

按[成果与提交标准](../06-deliverable-standards.md)整理本周成果，再由讲师按[监督与掌握度规范](../07-instructor-supervision.md)给出“通过、部分通过、未通过”。教程完成不代表学习者已经掌握，必须以独立运行、排错、Teach-back 和变体证据为准。
