# Agent 沙箱练习目录

本目录存放 Claude Agent SDK 和其他 Coding Agent 使用的安全练习项目。

- 可以提交专门构造的小型 Demo、Bug、测试和无敏感信息的 Fixture。
- `runtime/` 用于本地临时执行副本，其内容默认被 Git 忽略。
- 重要工作仓库、个人主目录和生产目录不应作为练习工作区。

每个可提交 Demo 应说明初始问题、允许操作、禁止操作、验收命令和预期结果。即使使用本目录，也仍需配置工具权限、路径限制和必要的容器隔离。
